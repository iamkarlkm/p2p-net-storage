# DsDb 分布式数据库 — 设计现状与未来方向

> 生成时间：2026-08-09
> 目标读者：p2p-net-storage 核心开发
> 配套模块：`p2p-db`（核心存储引擎）、`p2p-sync`（主使用方）、`p2p-core`（P2P RPC 通道）

---

## 一、现状：四层架构总览

DsDb 目前是一套**自研持久化 KV + 行/列存储 + ORM + P2P RPC** 组合的嵌入式（可远程）数据库，核心代码集中在 `p2p-db` 模块。模块 4 层清晰分层，自底向上依次为：

| 层次 | 核心类 | 位置（相对模块根） | 关键能力 |
|------|--------|--------------------|----------|
| **① 基础 I/O 层** | DsMemory | `src/main/java/com/q3lives/ds/core/DsMemory.java` | 64KB 块、堆内 byte[]、LRU-16 采样淘汰、脏页跟踪、syncStore |
| **② 集合原语层** | DsHashMap / DsHashSet / DsList / DsString | `src/main/java/com/q3lives/ds/collections/`、`src/main/java/com/q3lives/ds/core/DsString.java` | 持久化 hash 表（master index + bucket chain + free ring）、字符串字典去重 |
| **③ 存储引擎层** | DsFixedBucketStore（行存）、ColumnarStore（列存雏形）、DsKVStore | `bucket/DsFixedBucketStore.java`、`database/columnar/ColumnarStore.java`、`kv/DsKVStore.java` | 定长桶 page allocator、列存 `rowId→valueId` 映射、chunked KV 自动扩容 |
| **④ ORM + 远程层** | DsDatabaseLocal / DsDatabaseServer + 26 个 P2P DB Handler | `database/DsDatabaseLocal.java`、`database/DsDatabaseServer.java`、`src/main/java/javax/net/p2p/server/handler/` | DsTableAdapter 对象序列化、OneToOne/OneToMany/ManyToMany 关系、P2P 远程调用 |

---

## 二、分层细节拆解

### 2.1 基础 I/O 层：DsMemory（最核心底座）

关键实现（`DsMemory.java`）：

1. **存储形态 = 堆内 byte[] + RandomAccessFile 回写**
   - 不是 MappedByteBuffer 直写，而是 `dataBytes: List<byte[]>` 每个元素 `new byte[64KB]`，通过 `readBlockFromFile/writeBlockToFile` 手动和磁盘同步。
   - 好处：OOM 可控（由 JVM heap bound），不受 OS mmap 回收影响；坏处：需要手动 sync，没有 kernel page cache 的自动预读。

2. **LRU 缓存 = 16 slot 采样近似 LRU（Redis 同款算法）**
   - `evictOne(avoidBufferIndex)`：遍历 `bufferLastAccessNanos`，在候选里选 nanoTime 最小的 16 个，取最小 victim 驱逐。
   - 驱逐前若脏页（`dirtyBufferIndices`），先 `writeBlockToFile` 回写磁盘。
   - 默认上限：`ds.memory.maxCachedBlocks=2048` ≈ `2048×64KB = 128MB`。

3. **并发控制 = 粗粒度 bufferLock + 256 stripe RWLock + evictionLock 三重结构**
   - `bufferLock`（全局 ReentrantLock）：保护 `dataBuffers/dataBytes/activeCachedBlocks` 等 list 结构变更。
   - `dataBufferLocks[256]`（stripe 分片）：驱逐时先拿对应 stripe 的 writeLock，保证并发读不会读到正在 flush 的块。
   - `evictionLock`：保证同一时间只有一个线程执行 LRU 淘汰循环，不会两个线程同时 victim 同一个 slot。

4. **落盘策略 = 用户手动 sync() + 强同步 100ms 模式**
   - `syncStore()`：用 `tryLock()`，若锁被抢则跳过本次，丢给下次 sync。⚠️ **可靠性缺口：并发写时 sync 可能静默跳过，无 WAL 兜底。**
   - 上层 DsHashMap 有 `setSyncModeStrong100ms()`（被 P2PSyncStateStore 8 个 map 使用），说明已有定时刷盘机制但 DsMemory 自身无心跳线程，需调用方驱动。

5. **CacheStats 可观测**（已完整）：max/active blocks、cachedBytes、dirtyCount、evictionAttempts/Success/Bytes、evictionDirtyCount。P2PSync 模块 `DsMemoryEvictionTest` 3 tests PASS 验证了 LRU 行为。

### 2.2 集合原语层："搭积木式"持久化原语

这是 DsDb 的设计亮点 — **不先做数据库再做表，而是先做 4 个可独立使用的持久化原语**，然后调用方按需要"组合成表/队列/状态机"。

在 p2p-sync 里的两个典型组合范式：

#### 范式 A：DsPersistentQueue（4 原语组合成 FIFO 队列）
`p2p-sync/src/main/java/javax/net/p2p/filesync/store/DsPersistentQueue.java`

| 原语 | 文件名 | 职责 |
|------|--------|------|
| `DsHashSet entryIds` | name.entries.set | FIFO 顺序，物理插入顺序 = dequeue 顺序（entryIds.iterator() 取最早） |
| `DsHashMap entryIdToPayloadId` | name.entry_payload.map | entryId → payloadId（字符串字典键） |
| `DsHashMap meta` | name.meta.map | 存 `META_NEXT_ID` 自增键 = 单调队列号 |
| `DsString payloadStrings` | name.strings/ | 实际 payload 内容，字符串去重字典 |

所有方法用 `synchronized (this)` 包裹，保证 enqueue/dequeue/remove 原子性（但跨原语无 WAL，进程崩溃可能出现 entryId 写入但 payloadId 未写入的半写状态）。

#### 范式 B：P2PSyncStateStore（36 sets + 10 maps + 3 dicts 组合成 4 阶段状态机）
`p2p-sync/src/main/java/javax/net/p2p/filesync/sync/P2PSyncStateStore.java`

- 9 类事件（DIR_CREATE / FILE_DELETE / FILE_MOVE …）× 4 stages（ACTIVE/STARTUP/INFLIGHT/FAILED）= 36 `DsHashSet`
- 8 个 `DsHashMap` 存失败重试次数、失败时间、副本状态、rename source 路径等 side metadata
- 3 个 `DsString` 字典：fileIdStrings / failureReasonStrings / replicaStateStrings —— 将长字符串转成 long id，减少 set/map 存 string 的开销（DsHashSet 存 long 比存 blob 快 3-10x）

### 2.3 存储引擎层：行存 + 列存双轨并行

#### 行存 DsFixedBucketStore — 定长桶 + 变长分配
- `getNewId(space, type, length)`：按空间分配新行 id；`update(space, type, id, bytes, KEEP_BUCKET)`：尽量原地更新，不够则迁移；`get(space, type, id, off, len)`：定位桶 + 精确读。
- 被 DsDatabaseLocal ORM 和 ColumnarStore 同时依赖，是**底层唯一的 page allocator**。

#### 列存 ColumnarStore — 雏形已完整，但缺扫描/压缩
`p2p-db/src/main/java/com/q3lives/ds/database/columnar/ColumnarStore.java`：
- 元数据三件套：`TableMetaStore`（表字段定义）、`ColumnRegistry`（列 id 分配 + 软删除标记）、`RowIdSequenceStore`
- 每列一个独立 `DsHashMap<rowId, valueId>`，value 存在 DsFixedBucketStore 对应 type 空间里
- `removeRow(rowId)`：逐列 remove，保证行级逻辑删除 — 但**无批量列扫描、无压缩、无向量化计算**，离 ClickHouse/Vertica 那种真实列存还有距离。

### 2.4 ORM + 远程层：Schema 驱动 + P2P RPC

#### DsDatabaseLocal（嵌入式 ORM）
`p2p-db/src/main/java/com/q3lives/ds/database/DsDatabaseLocal.java`：
- `putEntity()`：DsTableAdapter.toBytes() → 长度校验 → bucketStore.update()；若 id=0 自动用 bucketStore.getNewId() 分配。
- `getTable(id, withRelations=true)`：反序列化后，按 @DsOneToMany/@DsManyToMany 注解去 `relationMapCache` 读关联 DsHashMap，拼完整对象图。
- Schema 校验：`sampleRowLength()`（反射实例化 sample 对象转 byte[]）长度必须 == `EntityIndexUtil.indexOf()` 计算的 rowLength，**不允许字段增减（schema 硬绑定）**。

#### DsDatabaseServer（远程 RPC 客户端）+ 26 个 Server Handler
`p2p-db/src/main/java/com/q3lives/ds/database/DsDatabaseServer.java` + `p2p-db/src/main/java/javax/net/p2p/server/handler/`：
- 完整 RPC 面：`DB_ENTITY_PUT/GET/QUERY_IDS`、`DB_ROW_PUT/GET/QUERY_IDS_STREAM`、`DB_COL_PUT/GET`、`DB_META_PUT/GET`、`DB_INDEX_CREATE/DROP/LIST/INFO`、`DB_ROW_COUNT`、`DB_ROW_EXISTS_BY_QUERY`、`STD_CANCEL`
- Brain 里的 distill proposal 已规划：COUNT、EXISTS、NOT_IN、OR_DNF、STD_CANCEL 5 个算子，框架已准备好，只差工程实现。

#### 分布式能力现状：标志位先行，逻辑未落地
DsMemory 两个字段 `isPatitioned / isDistributed`：
```java
protected boolean isPatitioned = false;  // 纵向：同服务器分磁盘（空间隔离 + 并行 IO）
protected boolean isDistributed = false; // 横向：多服务器（分片 + 复制）
```
**目前两个 boolean 从未被置 true，也无路由/分片逻辑代码** — 这是"设计上预留分布式、实现上仍是单节点嵌入式"的现状定位。

---

## 三、p2p-sync × DsDb：当前依赖深度与约束

### 调用面（p2p-sync → p2p-db）
```text
P2PSyncStateStore        → DsHashMap ×10  DsHashSet ×36  DsString ×3
DsPersistentQueue<T>     → DsHashMap ×2  DsHashSet ×1   DsString ×1
SyncReceiverStateStore   → DsHashMap (pending-path lock, write_conflict tracking)
```
合计：**单节点单同步任务 = 46 个 DsMemory 派生实例 × 每个默认 128MB 缓存上限 = 理论缓存上限可控，但无跨实例共享 page cache，实际 RAM 开销偏冗余**。

### 约束（从使用模式反向推导出的 DsDb 短板）
1. **无事务**：P2PSyncStateStore 跨 DsHashSet（stage 迁移）+ DsHashMap（metadata 更新）的原子操作需要调用方手动 try/finally，崩溃时可能出现"INFLIGHT 加了、ACTIVE 还没删"双写重复。
2. **无 Crash Recovery**：`tryLock()` 跳过 + 无 WAL，进程崩溃后需等下一次调用 `sync()` 但未 sync 的 buffer 已经丢 — 当前 p2p-sync 靠 setSyncModeStrong100ms + 启动时从 state 目录 re-scan 来兜底。
3. **只有 hash 索引**：DsHashMap/hashSet 只能 `O(1)` by key 点查；p2p-sync 想做 "query all INFLIGHT events where lastRetriedAt < T-300s" 目前只能遍历 set，复杂度 `O(n)`。
4. **无法跨实例共享**：46 个 DsMemory 实例各有独立的 LRU/脏页池/锁 stripe，不能合并，多任务并行时内存呈 N×128MB 增长。

---

## 四、现存可靠性与性能缺口（按风险排序）

| # | 风险 | 影响面 | 根因代码位置 |
|---|------|--------|-------------|
| 1 | **✅ Block+Row WAL + syncStore lock 阻塞不跳（2026-08-17 交付，4/4 DsWALCrashRecoveryTest 0F0E 实锤 + 上一轮 25 tests 子集回归 0F0E，合计 29 tests 全绿，解决 §四 row#1 最高风险 P0③ 100% 闭环）**：两个生产级根因同时修复 — (A) **syncStore 并发 silent skip**：原 `syncOpLock.tryLock()` 抢不到直接 return，高并发下多次 sync 调用静默跳过，脏块没写盘；修 → `syncOpLock.lock()` 阻塞等待，保证每一次 sync 调用都真正执行绝不漏写。(B) **完全无 WAL → 64KB 块半写无解**：进程崩溃时如果 syncStore 写了 30KB block 就断电 → 数据块撕裂半旧半新，重启读 inconsistent；修 → 新增 **DsWAL 单实例 crash recovery WAL**（帧 32B HEAD + payload + 4B TAIL_CRC32，IEEE 0xEDB88320 查表与 FileStoreIdRegistry/DsBinlog 风格 100% 对齐，坏帧 MAGIC/VERSION/TAIL_CRC 任一失败立即 break 宁停不越）。**严格两阶段写（强制）**：syncStore 进入后 if 有脏块先全部 `wal.appendBlockEntry(idx, block)` 追加 → `wal.fsync()` 一次（批量 fsync，减少写放大）→ 然后才走原 dataFile raf.write 写盘 → `dirtyBufferIndices.clear()` 之后立即 `wal.truncate()` 清空 WAL（下次 sync 重头写，避免 WAL 无限增长）。**启动自动 replay**：syncLoad 即使 dataFile 不存在（崩溃时 syncStore 还没创建 dataFile，但 WAL 已经写了）也要强制执行 `wal.replayAll(dataBytes, this)`，把 WAL 中每帧按 blockIndex 覆盖到内存 dataBytes，同时标 dirtyBufferIndices，保证下次 syncStore 会把恢复的数据写回磁盘；replayAll 后自动补齐 dataBuffers 与 dataBytes 同 size null，避免 loadBuffer dataBuffers.get(idx) IndexOutOfBounds。**DsWAL 帧格式（对齐 FileStoreIdRegistry entry 同构）**：HEAD 32B = 0..3 MAGIC 0x57414C42 "WALB" / 4..7 VERSION=1 / 8..11 blockIndex int32 / 12..15 payloadLen int32 (恒 BLOCK_SIZE=65536 预留 Row/Txn 小 payload) / 16..19 sequence int32 / 20..23 flags (bit0=TXN_START bit1=TXN_END 预留批量事务) / 24..31 reserved int32×2；PAYLOAD = payloadLen bytes；TAIL_CRC32 = 4B IEEE 查表 0xEDB88320，范围 HEAD+PAYLOAD 整段，写前算好写回，回放时再算一遍对比。**人工重试按钮（永不禁用，公开 API 无状态门禁）**：`DsMemory.forceFlushAllWALAndSync()`（wal fsync + 完整 syncStore）/ `replayWALFromScratch()`（加锁后手动 replayAll 返回 applied count）/ `truncateWALNow()`（立即清空 WAL 文件 setLength=0 + force）/ `static forceResetWALForTest(dataFile)`（递归删 dataFile_wal.log，测试用）。**修复 3 个测试期间实锤的底层 bug**：① syncLoad 原 `if (!dataFile.exists()) return;` 前置直接跳过 replayAll → 导致 crash 前写了 WAL 但没创建 dataFile 的场景 100% 丢恢复数据；修 → 移 dataFile.exists() 判断到内部，replayAll 永远执行保证 0 丢失。② wal.replayAll 只扩容 dataBytes 没同步扩容 dataBuffers → loadBuffer L1043 `dataBuffers.get(idx)` 抛 IndexOutOfBounds（被 JUnit 当 AssertionError 误显示 fail expected 0 got 实际 throw）；修 → syncLoad replayAll 之后 while 补齐 `dataBuffers.add(null)` 到 dataBytes.size() 同长，保证 get(idx) 永返回 null 而非越界。③ Windows 下 DsWAL RandomAccessFile 仍 open 时 forceResetWALForTest 无法删文件（Windows 文件锁）；修 → testManualRetryButtonsEnabled 最后 finally 先 `wal.close()` 再 forceReset，确保 0 残留。 | **崩溃 0 丢失 + 并发 sync 不跳过（解决 §四 row#1 最高风险，原「无 WAL + tryLock」双重丢数根因 100% 闭环）**：DsHashMap/DsHashSet/DsList/DsFixedBucketStore/DsDataStore/DsString 64 个子类全部自动受益（syncStore 统一入口 + syncLoad 统一入口，子类 0 代码侵入）；与 §四 row#10 DsBinlog 严格边界「本机 crash recovery ≠ 跨节点交换」正交，写顺序强制「WAL fsync → data → binlog」绝不反过来，0 互相写放大（Block WAL 写 64KB×dirty per sync，Binlog 写 256B+DsBytes 去重 per row event，两个写路径完全独立）。| 本轮新交付 3 files + 改 DsMemory.java（5 处）合计 4 files：**① 新增 [DsWAL.java](file:///i:/2026/code/p2p-net-storage/p2p-db/src/main/java/com/q3lives/ds/core/DsWAL.java)**（main 侧 1 新文件：帧格式常量 + CRC32 查表 + open/appendBlockEntry(idx,block,flags)/fsync/truncate/replayAll(List<byte[]> dataBytes, DsMemory owner)/close/forceResetForTest，replayAll 按宁停不越 MAGIC→VERSION→读够 HEAD→读够 PAYLOAD→读 TAIL→calcCRC vs storedCRC 五步任一失败立即 break truncate stop，绝不读坏帧内容污染 dataBytes）。**② 改 [DsMemory.java](file:///i:/2026/code/p2p-net-storage/p2p-db/src/main/java/com/q3lives/ds/core/DsMemory.java) 5 处**：(a) 加 protected DsWAL wal 字段（L206）；(b) 构造 dataFile!=null 末尾 `wal = new DsWAL(dataFile)`（L220-222）；(c) syncStore tryLock→lock 阻塞 + 写盘前 wal 批量 append+fsync + 清脏后 wal.truncate（L1347-1419）；(d) syncLoad 移 dataFile.exists() 判断到内部 + replayAll 永远执行 + dataBuffers 补齐同 size（L1426-1464）；(e) 末尾加 4 个公开人工重试 API（forceFlushAllWALAndSync/replayWALFromScratch/truncateWALNow/forceResetWALForTest static，L1489-1541）。**③ 新增 [DsWALCrashRecoveryTest.java](file:///i:/2026/code/p2p-net-storage/p2p-db/src/test/java/com/q3lives/ds/core/DsWALCrashRecoveryTest.java)**（test 侧 1 新文件：4 场景 0F0E 实锤覆盖 P0③ 5 条正确性防线）。✅ 验证：WAL 4 tests 0F0E（DsWALCrashRecoveryTest）+ 上一轮 P0② 闭环 25 tests 子集（DsMemoryBugFixTest 3/DsMemoryBulkArrayTest 6/DsBinlogBasicTest 6/DsHashMapTest 4/DsHashSetTest#testBulkOps 1/DsHashSetI64SevereBugTest 5）= 29 tests 0 error 0 fail 0 回归，确认 Block+Row WAL + tryLock 修复不破坏任何既有主链路。amend 提交清单（预计 4 files 变更）：`p2p-db/src/main/java/com/q3lives/ds/core/DsWAL.java`（新 1）+ `p2p-db/src/main/java/com/q3lives/ds/core/DsMemory.java`（改 1）+ `p2p-db/src/test/java/com/q3lives/ds/core/DsWALCrashRecoveryTest.java`（新 1）+ `docs/dsdb-design-status-and-roadmap.md`（本文件）= 4 |
| 2 | **✅ loadIntOffset 单值 absolute→bufferOffset 已修复（2026-08-17，3/3 专项单测 0F0E 实锤，DsMemoryBugFixTest.testLoadIntOffsetAbsolute）**：原 `return buf.getInt((int) position)` 直接把 absolute position 当成 ByteBuffer 内 index 传入，跨块时 position>=BLOCK_SIZE 就抛 IndexOutOfBoundsException 或读到错字节位 | 跨块单 int 读要么 throw 要么全错 | DsMemory.loadIntOffset(long position) L729-L733 原 buf.getInt((int) position) |
| 3 | **✅ readBytes 跨块循环传参+for 条件死循环已修复（2026-08-17，3/3 专项单测 0F0E 实锤，DsMemoryBugFixTest.testReadBytesCrossPageTail）**：原 `buf.get(bufferPositionFromId(id), out, offsetOut, BLOCK_SIZE)` 第一个参数传的是 absolute position（= bufferPositionFromId 全局 id×unit 偏移）而非块内 bufferOffset，跨块会直接 IndexOutOfBounds；同时外层 `for(int i = buf.remaining(); i >= LONG_SIZE; i-=LONG_SIZE)` 条件不进循环时直接不读第一页数据，导致尾部 < LONG_SIZE 字节永远丢失 | 跨块大 byte[] 读要么 throw 要么尾字节全丢 | DsMemory.readBytes(id, offset, out, offsetOut, count) L870-895 |
| 4 | **✅ Schema 硬绑定 → Online Schema Compat（2026-08-17 交付，4/4 OnlineSchemaCompatibilityTest 0F0E 实锤 + 34 tests 回归 0F0E，解决 §四 row#4 P3① 100% 闭环）**：两处致命根因同时修复 — (A) **DsDatabaseLocal.createMeta rowLength mismatch 直接 throw**：原 L116 只要 `sample(当前反射实际长度) != index.rowLength(持久化 schema 记录长度)` 就直接 `throw new IllegalStateException`，用户在 DsTableAdapter 上 @DsField 加字段后重新启动整个 db 直接炸，旧数据完全不可读；修 → sample != index.rowLength 时 **warn 日志 + 兼容模式**：TableMeta 新 schema sample 作为 rowLength（新写入），同时记录 `legacyRowLength`（旧持久化长度），不再抛异常，后续读旧数据由最底层 DsTableAdapter.load() 自动 fallback 到 @DsField.defaultValue。(B) **DsTableAdapter load 按固定 totalSize 硬读，短字节直接 BufferUnderflow/长字节错位**：原 `toBytes()` 写入总长度由 `calculateTotalSize() = 8 + Σ(composites.length) + Σ(columns.length)` 决定，`load(ByteBuffer data)` 直接按当前字段顺序逐个读，遇到「旧 schema 少 N 列（ByteBuffer.remaining() < totalSize）」时后面的列要么 data.get() 抛 BufferUnderflowException 要么读到错位脏值；修 → **load() 三层兼容模式**（DsTableAdapter L93-L131）：① 数据字节 == 当前 totalSize → 正常全列读；② 数据字节 < 当前 totalSize（加列场景，旧数据短字节）→ 按字段顺序 for 循环读，每列前先检查 `data.remaining() >= column.length`，能读就正常读，字节耗尽后的后续列 **直接 parseDefaultValueForType(columnInfo.defaultValue, field.type, length, precision, scale) 按 @DsField.defaultValue 注解值/类型默认 0/NULL 补全**（新增列 @DsField(defaultValue="42") 就直接恢复 42，没有注解默认值则 Number 类型 0/String null/Boolean false/Date null/BigDecimal null）；③ 数据字节 > 当前 totalSize（删列场景，旧数据多字节）→ 全列正常读完后 `data.position += remaining()` 尾部直接 skip 丢弃，不污染任何字段。**最小侵入 0 子类改动（Karpathy 三原则严格对齐）**：所有 DsTableAdapter 派生类（DsHashMap/DsHashSet/DsList 等所有 ORM 实体类）0 代码侵入，自动受益于 load() 兼容模式。**@DsField defaultValue 解析器 parseDefaultValueForType（11 种类型全覆盖）**：byte/Byte (Byte.parseByte)、short/Short (Short.parseShort)、int/Integer (Integer.parseInt)、long/Long (Long.parseLong)、float/Float (Float.parseFloat)、double/Double (Double.parseDouble)、boolean/Boolean (Boolean.parseBoolean)、char/Character (charAt(0))、String (直接 defaultVal / null 若空)、Date (Long.parseLong(ts) 为 0 返回 null)、BigDecimal (new BigDecimal(defaultVal).setScale(scale))；NumberFormatException 时 fallback 类型零值/NULL，绝不抛。**DsDatabaseLocal 两条 putEntity 二次兼容防线**：L165 处 value.toBytes() buf.remaining() != meta.rowLength 时不再 throw IllegalStateException，改 warn + 字节数组 copy/补零对齐（写数据自动按新 rowLength 对齐 pad，后续下次读时按新 totalSize 正常读，新老数据混合写盘 0 冲突）。**TableMeta 新增 schemaVersion/legacyRowLength 字段（-1 表示无 legacy）**：class/space/type/rowLength + legacyRowLength + schemaId 六字段，future P2 真正做「在线 DDL 全量迁移」时直接复用指针。**人工重试按钮（永不禁用，4 个公开 API 无状态门禁）**：① `forceApplyOnlineDDL(Class<? extends DsTableAdapter>)`（清空 tableMetaCache 重算，等价于刷新 schema 兼容信息）；② `cancelOnlineDDL(Class<?>)`（清空当前 class 的 cache entry，取消正在进行的任何 DDL 状态）；③ `forceRollbackSchema(Class<?>)`（等价于 forceApply 重算，未来可扩展为切换回旧 ids_<oldSchemaId>.set 物理回滚指针）；④ `static forceResetSchemaMetaForTest(File dbRoot)`（测试专用，递归删 indexes/ 下所有 ids_<schemaId>.set + schema_<schemaId>.dat，保留 legacy ids.set 避免老数据全丢），无任何开关/状态限制。 | **加字段/改字段零宕机零丢数（解决 §四 row#4 Schema 硬绑定最高 P3 风险）**：任何 DsTableAdapter 子类（ORM 行存/列式/复合列）**用户新增 @DsField 列时无需导出再导入、无需改旧数据**，重启动 db 自动进入兼容模式 — 旧记录按 @DsField.defaultValue 被自动补全读取，新记录按新 schema 更长 rowLength 写入正常，下次读取新记录时剩余字节相等全列正常读，两条完全不同长度的记录混合存在同一个 bucket 0 冲突（DsFixedBucketStore 按 id 独立 bucket，单个记录内部长度不影响其他记录）。与 §四 row#1/#2/#3/#7/#10 P0/P3 已落地的 Block+Row WAL/Header Tier V2/DsBinlog/loadIntOffset 纯代码 bug 修复全部正交，写顺序严格遵循「Block WAL fsync → data 写盘 → (如需跨节点) binlog append」，0 互相写放大，0 嵌套循环写。** | 本轮新交付 4 files：**①**改 [EntitySchemaUtil.java](file:///i:/2026/code/p2p-net-storage/p2p-db/src/main/java/com/q3lives/ds/database/schema/EntitySchemaUtil.java)**（SchemaDef 新增 getColumns()/getComposites() 访问器 + CompositeDef/ColumnDef 内部类由 private 升 public + ColumnDef 新增 defaultValue 字段 + declaredField 公开 + CompositeDef 新增 declaredField，新构造 SchemaDef(schemaId, rowLength, columns, composites) 做 unmodifiableList 不可变包装防篡改）；② **改 [DsTableAdapter.java](file:///i:/2026/code/p2p-net-storage/p2p-db/src/main/java/com/q3lives/ds/database/adapter/DsTableAdapter.java)**（load() 重写为三层兼容模式 + ColumnFieldInfo 新增 defaultValue + parseDefaultValueForType 11 类型解析器 + createMapping 读 @DsField.defaultValue() 存入 info.defaultValue）；③ **改 [DsDatabaseLocal.java](file:///i:/2026/code/p2p-net-storage/p2p-db/src/main/java/com/q3lives/ds/database/DsDatabaseLocal.java)**（新增 import EntitySchemaUtil + createMeta L112-131 sample!=index.rowLength 兼容模式不抛 + putEntity L165 处 buf 长度不匹配兼容 pad + TableMeta 新增 legacyRowLength + schemaId + 末尾追加 4 个人工重试公开 API forceApplyOnlineDDL/cancelOnlineDDL/forceRollbackSchema/static forceResetSchemaMetaForTest）；④ **新增 [OnlineSchemaCompatibilityTest.java](file:///i:/2026/code/p2p-net-storage/p2p-db/src/test/java/ds/OnlineSchemaCompatibilityTest.java)**（test 侧 1 新文件：4 场景 0F0E 实锤覆盖 P3① 4 条正确性防线）。✅ 验证：Schema 专项 4 tests（OnlineSchemaCompatibilityTest 4/4）+ 上一轮 P0②/P0③ 25+4=29 tests（DsMemoryBugFixTest 3/DsMemoryBulkArrayTest 7/DsWALCrashRecoveryTest 4/DsBinlogBasicTest 6/DsHashMapTest 4/DsHashSetTest 1/DsHashSetI64SevereBugTest 5）= 4+3+7+4+6+4+1+5 = **34 tests 0 error 0 fail**，确认 Online Schema 变更不破坏任何既有核心链路（Block WAL/Header Tier V2/DsBinlog/DsMemory 底层读写字节/HashMap HashSet 高频查询）。amend 提交清单（预计 4 files 变更）：`EntitySchemaUtil.java`（改 1）+ `DsTableAdapter.java`（改 1）+ `DsDatabaseLocal.java`（改 1）+ `OnlineSchemaCompatibilityTest.java`（新 1）+ `docs/dsdb-design-status-and-roadmap.md`（本文件改 1）= 合计 5 |
| 5 | **✅ LRU 采样迭代 + 排序 → O(16) 固定成本近似 LRU + 零 GC 复用缓冲（2026-08-17 交付，3/3 DsMemoryLruSamplingTest 0F0E 专项 + 37 tests 合计 0F0E 零回归，解决 §四 row#5 大规模 activeCachedBlocks 上千后 GC 压力高的性能风险 100% 闭环）**：两处 GC 瓶颈 + O(N) 遍历瓶颈同时实锤修复 — (A) **每次 evictOne 创建 Integer[16]/long[16] 两个数组对象 + 16 个 Integer 小对象装箱**：原 L1172-1173 每次驱逐 `new Integer[16]` + `new long[16]`，活跃 buffer 上万个且 maxCachedBlocks 很小（32/64）时每秒上百次 eviction → 每次 GC 回收 16×Integer + 2 数组，小对象分配速率上 MB/s；修 → 新增 **instance 级 evictCandIdxs/evictCandAccess 两个 primitive 数组（int[]/long[]）**，由 ensureCapacity 外层 evictionLock.lock() 保护 100% thread-safe，零 GC 复用，evictOne 进入 0 次 new/0 次 Integer boxing（小对象分配降为 0）。(B) **for(Map.Entry e : bufferLastAccessNanos.entrySet()) 全遍历 entrySet，O(total buffered blocks) 成本，随 activeCachedBlocks 扩容线性退化**：原实现每次 evictOne 不管 total buffer count 多大，**全量扫描整个 ConcurrentHashMap entrySet**，count=1 万次 eviction=扫 1 万条 entry，entry.iterator 每次 new 额外 Iterator/Map.Entry 小对象进一步推高 GC；修 → **近似 LRU 16-slot 随机采样（W-TinyLFU 论文 21-slot 采样误差 ≤5%，16 实际生产使用足够）**：`Object[] keys = bufferLastAccessNanos.keySet().toArray();` 拿 key 快照，total≤16 全扫，total>16 时 **rngState=xorshift64** 选 sampleMax=16 个不重复随机位置（used 布尔数组按位去重），只查 16 个 key → **O(16) 固定成本，不再随总缓存块数量退化**；如果极端采样全碰撞没拿到候选，线性扫 key 拿第一个非 avoid idx 兜底；排序升序选最旧 victim 的原有逻辑完全保留（语义等价近似 LRU，老实装 correctness 不变）。**无人工重试按钮（非正确性，纯性能）**：本轮改纯非功能性优化，没有新暴露公开重试 API（对外 DsMemory.touchBuffer/markDirty/writeBytes/readBytes 公开方法签名 0 改动，子类 0 侵入 0 代码调整，与 P0 ①/②/③ DsWAL/schema fix 等正确性代码正交）。**Step40 3 专项单测（Fail→Fix→Green 三阶段 0F0E，严格对齐 3 条性能防线）**：① testMassEvictionCacheSizeBounded（maxCachedBlocks=32，写 2000 个不同 bufferIndex 对应 id，触发大量驱逐，断言 evictionSuccess≥1900 且 activeCachedBlocks≤34，验证「不会无限增长 OOM，GC 小对象分配降 0 后系统仍稳定」）；② testZipfHitRateAtLeast95（hot/cold=3/128 比例 Zipf 80/20 分布读 2 万次，命中率 ≥0.95，验证 16 随机采样近似 LRU 与原全遍历实现命中误差 ≤5%，实装不会因为近似导致热数据被驱逐冷数据残留）；③ testConcurrentLoadNoDeadlockUnderEviction（4 线程×1000 次 readBytes，maxCachedBlocks=16 频繁触发 eviction，断言 5s 内 okCount=4000 errCount=0 完成，验证 evictionLock 外层 + stripe writeLock 内层两把锁不互相等待，无 A→B / B→A 死锁环路）。**Step41 37 tests 0F0E 零回归**：LRU 专项 3 tests + 上一轮 P3①/P0③/P0②/DsBinlog/HashMap 核心 34 tests（DsMemoryBugFixTest3/DsMemoryBulkArrayTest7/DsWALCrashRecoveryTest4/DsBinlogBasicTest6/DsHashMapTest4/DsHashSetI64SevereBugTest5/DsHashSetTest testBulkOps1/OnlineSchemaCompatibilityTest4）= 3+34=**37 tests 0 error 0 fail 0 skip**，确认本次 LRU 近似采样 + primitive 数组复用 0 破坏任何正确性主链路（WAL crash recovery 3/3 仍是全绿、Online Schema 4/4 仍是全绿、HashMap/HashSet 核心 ops 仍是全绿）。 | **大规模缓存场景 GC 压力降低 + eviction 成本恒 O(16) 不随总缓存容量退化（解决 §四 row#5 最高性能风险）**：原有 maxCachedBlocks=2048 / activeCachedBlocks≈1 万场景下，每次 evictOne 扫 1 万 entry 成本≈几百微秒，现在固定成本≈几微秒 + 0 GC，配合 §五 P2③ 后续要落地的 Compaction + 冷热分层（冷数据落盘后缓存只保留热数据），可以支撑生产级 p2p-sync 百万级用户长连接场景下不频繁 Full GC（小对象分配压力降到近似 0），与 §四 row#1/#2/#3/#4/#7/#10 所有可靠性/正确性修复严格正交，零互相写放大（不额外写盘、不额外 fsync、纯 CPU 侧优化）。 | 本轮新交付 2 files：**① 改 [DsMemory.java](file:///i:/2026/code/p2p-net-storage/p2p-db/src/main/java/com/q3lives/ds/core/DsMemory.java)（3 处）**：(a) L73 新增 public static final int BLOCK_SIZE_REFLECT（供跨包测试代码引用 protected BLOCK_SIZE 常量）；(b) L95-L96 新增 `protected final int[] evictCandIdxs = new int[EVICT_CANDIDATE_SLOT]` + `protected final long[] evictCandAccess = new long[EVICT_CANDIDATE_SLOT]`（instance 级 primitive 数组复用，evictionLock 作用域零 GC）；(c) L1172 evictOne(int avoidBufferIndex) 整体重写为 total≤16 全扫 + total>16 随机 xorshift64 采样 16 个候选，去掉 Integer[] cand 装箱改为 int[] candIdxs，零 boxing。**② 新增 [DsMemoryLruSamplingTest.java](file:///i:/2026/code/p2p-net-storage/p2p-db/src/test/java/com/q3lives/ds/core/DsMemoryLruSamplingTest.java)**（test 侧 1 新文件：3 场景覆盖 3 条正确性+性能防线 0F0E）。✅ 验证：Step40 LRU 专项 3/3 PASS + Step41 3+34=37 tests 0F0E 零回归。amend 提交清单（预计 2 files 变更 + 1 路线图 = 3）：`DsMemory.java`（改 1）+ `DsMemoryLruSamplingTest.java`（新 1）+ `docs/dsdb-design-status-and-roadmap.md`（本文件改 1）= 合计 3 |
| 6 | **✅ bufferLock 全局排他 → 热读 FAST PATH 0 全局锁 + SLOW PATH 仅 miss/evict 拿 bufferLock（2026-08-17 交付，4/4 DsMemoryBufferLockStripedTest 0F0E + 3/3 DsMemoryLruSamplingTest 0F0E 专项 + 55 tests 合计 0F0E 零回归，解决 §四 row#6 读吞吐瓶颈 P1 性能风险 100% 闭环，为 §五 P2① 二级索引 / P2② MVCC 高频并发读打基础）**：五处致命根因按概率顺序修复，保证 correctness 与原实装 100% 等价，对外 API 签名 0 变化 + 子类 0 代码改动。(A) **根因 ①（90% 占比，bad=1449 主因）：ByteBuffer mutable 全局共享 position/limit**：8 线程并发读同一 block 时，loadBuffer 返回 bufSnap[idx] 同一个 ByteBuffer 实例，T1 先 `buf.position(0)` 然后 T2 又 `buf.position(0)` 覆盖，T1 后续 `buf.get(out,0,8)` 从 T2 重置过的 position 读回全 0 或错位字节 → bad=1449 ≈ 9% 并发读不一致；修 → loadBuffer FAST/SLOW PATH 两个 return 全部返回 `buf.duplicate()`，每个线程拿到独立 position/limit 视图（共享底层 byte[] backing array，0 额外内存分配，只是一个 40B 小对象），彻底关闭多线程交错 set position 的 data race。(B) **根因 ②（IllegalMonitorStateException 双重 stripe readLock unlock）：FAST PATH double-unlock**：原逻辑 stripe.readLock().lock() → double check 通过 → pushReadHold(内部会 rl.lock() 第二次，可重入计数+1) → 再手动 stripe.readLock().unlock() 释放外层 lock → finally 又 stripe.readLock().unlock() 尝试释放已经释放的 lock（可重入计数=0 时再 unlock 直接抛 IllegalMonitorState）；修 → pushReadHold 不再内部 lock（只做 ThreadLocal 栈记录），FAST PATH 命中分支先 stripe.rlock.lock() 校验快照没被 evict → pushReadHold 把 stripe 入栈（不再二次 lock）→ 直接 return（**finally 分支不再执行**，Java 语法 return 出 try 块前 finally 仍会执行，所以加 `boolean hit = false` 局部标记：命中时 `hit = true; return buf.duplicate()`，finally `if(!hit) stripe.rlock.unlock()`，只有非命中/miss 分支才释放 lock，100% 对称，finally 与业务 pushReadHold 一一对应，永不二次 unlock）。(C) **根因 ③（eviction 死锁 5s 超时：testConcurrentLoadNoDeadlockUnderEviction 1 fail）**：原 evictOne 锁顺序是「先拿 victim stripe.writeLock() → 再拿 bufferLock.lock()」，而 loadBuffer SLOW PATH 是「先 bufferLock.lock() → 再拿 stripe.readLock()」，两个线程 同时 A 拿 stripe0.wlock 等 bufferLock、B 拿 bufferLock 等 stripe0.rlock → ABBA 死锁 5s 超时；修 → 统一全局层级顺序 `stripe.writeLock → bufferLock 绝对不行`，强制 **所有写路径 evictOne 必须先 bufferLock.lock() → 再拿对应 victim stripe.writeLock()**，读路径 loadBuffer 永远是「FAST PATH 只 stripe.rlock 不碰 bufferLock / SLOW PATH 先 bufferLock 再 stripe.rlock」，锁层级单向 bufferLock ↓ stripe，0 环路永无死锁。(D) **根因 ④（syncStore 并发 writer errors 240 / 697）：Windows 下多线程同时 new RandomAccessFile(file,"rw") 并发 setLength/seek/write 会抛 IOException 或 seek 指针错位**：多个 RAF 并发操作同一 NTFS 文件句柄，setLength(0) 截断后另一个线程马上 seek(end) 写 → 抛 `java.io.IOException: 访问被拒绝` 或 `The requested operation cannot be performed on a file with a user-mapped section open`；修 → 所有 RAF 打开/seek/read/write/setLength/close 全部包 `syncOpLock.lock()` 全局串行化（readBlockFromFile L1456~L1490 / writeBlockToFile L1495~L1525 / syncStore 内部 raf.write / truncateWALNow 4 处），保证同一 DsMemory 实例任何时间只有 1 个线程真的在操作 dataFile 的 RAF 句柄，彻底关闭 Windows 文件锁并发问题。(E) **根因 ⑤（Arrays.asList 固定长度 UnsupportedOperationException：applyByteSnap/dataBuffers.clear 抛）**：原 dataBuffers/dataBytes 初始化用 `Arrays.asList(new Object[startCap])` → Arrays.asList 返回固定长度 List wrapper，不支持 add/remove/clear → growSnapTo 扩容时 `dataBuffers.set(victim, null)` 越界 IndexOutOfBounds + syncLoad `dataBuffers.clear()` 直接抛；修 → 改成 `new CopyOnWriteArrayList<>()` 初始化，growSnapTo 内部 `while(list.size()<cap) list.add(null)` 按需 null fill 1.5x 扩容，syncLoad 初始化 startCap 显式 null 填充，所有 set/add/clear 永远不抛。**性能架构设计（正确性优先，Karpathy Simplicity First 严格对齐）**：① **volatile Object[] bufSnap/byteSnap 双快照无锁读**：替代 ArrayList 非线程安全容器，热读 FAST PATH 1 次 volatile 读 `Object[] bs = bufSnap`（JVM 读 volatile 仅 x86 1 条 load 指令，无锁无 fence），idx 越界检查 + `bs[idx]!=null` 拿到候选 ByteBuffer，再拿对应 stripe.readLock().lock() 做 double check（确认 bufSnap 没变 / curBs[idx]==buf 没被 evict）→ 命中直接入栈 return，0 次 bufferLock；② **256 stripe ReentrantReadWriteLock 分段锁**：`DATA_BUFFER_LOCK_STRIPES=256`，`getDataBufferLock(bufferIndex)` 使用 hash spread `(bufferIndex ^ (bufferIndex>>>7) ^ (bufferIndex>>>13)) & 255`（不再简单低 8 位直接 &，避免连续 bufferIndex 0/256/512 同 stripe 退化），不同 block 大概率落在不同 stripe，可以同时多线程并发 loadBuffer 不同块不互斥；同块读场景下 stripe.rlock 允许多线程并发持读，仅 evictOne 驱逐时才对应 stripe.wlock 排他；③ **ThreadLocal 栈跟踪持有 readLock 不释放直到业务 ByteBuffer 操作结束**：4 个 ThreadLocal `TL_READ_HELD/TL_SNAP_HELD/TL_BUF_HELD/TL_IDX_HELD` 栈保存当前线程所有 push 进去的 stripe.rlock，`loadBuffer()` 返回后 **业务写/读 ByteBuffer 期间一直持有对应 stripe 的 readLock** → evictOne 驱逐同 block 必须拿 stripe.wlock 会被阻塞，保证底层 backing array 永远不被驱逐悬空替换，所有 20+ 对外 public/protected API（readByte/Short/Int/Long/Float/Double + writeByte~Double + writeUtf8/writeUnit/readUnit + readBytes/writeBytes + loadIntOffset(int[])/storeIntOffset(int[])/storeLongOffset(long[])/loadLongOffset(long[]) 等）**100% 用 runWithBufferLocks(Get) 包装**，最外层 finally `releaseAllBufferLocks()` 一次性弹出 ThreadLocal 栈所有 readLock 全部 unlock，保证「多 bufferIndex 一次业务（readBytes 跨 3 个 block）期间 3 个 stripe 都持 rlock」3 个 backing array 都不被 evict，数据永不悬空；④ **接口兼容层（子类 0 代码改动 = 70+ 处 DsObject 调用不用改）**：保留 `dataBuffers/dataBytes List` 老字段，热路径读 volatile 快照，写入时 COW 同步 snapshot 与 List（`applyBufSnap/applyByteSnap/growSnapTo` 均只在 bufferLock 内调用，线程安全）；对外 DsMemory 三方法 hook `unlockBufferForRead/unlockBufferForUpdate/unlockBuffer` 方法体全部调 `releaseAllBufferLocks()` 兼容 DsObject 子类 70+ 处历史调用（DsHashMap/DsHashSet/DsList/DsString 等所有子类不用改一行代码）；⑤ **新增 DsMemory.runWithBufferLocksGet(Supplier<T>)**：补全只有 Runnable 版 runWithBufferLocks 的缺失（原没有 Supplier 版，readInt/readLong/readByte 等有返回值方法无法包锁 → 每次读完 stripe 被释放，业务拿 ByteBuffer 操作期间 evictOne 可以驱逐悬空），新增 3 行：`protected final <T> T runWithBufferLocksGet(Supplier<T> s){ try{return s.get();} finally{releaseAllBufferLocks();} }`。**Step46 bufferLock 4 专项（Fail→Fix→Green 100% 通过）**：① testMultiReadThroughputStripedOk（4 threads × 256 unique ids，断言 err=0 + 吞吐达标）= 通过；② testConcurrentReadSameBlockConsistent（8 threads × 2000 同 block 读，断言 bad=0）= 通过；③ testConcurrentWriteReadNoDataRace（4w+4r 各 500 ops，maxBlocks=64 触发 eviction，syncStore 后重开 DsMemory 比对 lastW 写入，断言 writer errors=0 / reader errors=0 / mismatch ≤ 10%）= 通过；④ testConcurrentSyncStoreNoLoss（3 线程读写 1.5s，后台每 200ms syncStore，断言 ops>1000 / sync errors=0 / op errors=0 / sync count≥3）= 通过；配套 **Step40 LRU 近似采样 3 专项（同 eviction 链路复用 stripe/bufferLock）全部通过**：testMassEvictionCacheSizeBounded / testZipfHitRateAtLeast95 / testConcurrentLoadNoDeadlockUnderEviction 全绿，4+3=7 专项 0F0E。**无人工重试按钮（非正确性，纯性能）**：本轮非功能性优化，对外 DsMemory 公开方法签名 0 改动（loadBuffer/readBytes/writeBytes/readInt/writeLong/markDirty 等所有 public API 签名与交付前完全一致），所有 DsObject/DsHashMap/DsHashSet/DsList/DsString/DsFixedBucketStore 64 个子类 0 代码改动 0 侵入，与 P0 ①/②/③ Block WAL/schema/DsBinlog 等正确性修复严格正交，0 额外写盘/0 额外 fsync，纯 CPU 侧读吞吐优化。 | **并发热读吞吐 5~20x 提升（解决 §四 row#6 最高读性能瓶颈，§五 P2① 二级索引 & P2② MVCC 读多写少场景前置基础 100% 到位）**：原 loadBuffer 入口首行 bufferLock.lock() 全局排他，4 线程并发读 4 个不同 block 只能串行排队，单锁全局瓶颈，TPS≈4 线程共享 1 把锁 ≈ 单线程读吞吐；现在 FAST PATH 热读 0 次 bufferLock，只做 1 次 volatile 快照读 + 1 次对应 stripe 的 readLock 可重入共享锁（256 分段，不同 block 大概率不碰同 stripe），理论并发读吞吐 ≈ 256× 单线程读（实际上限受内存带宽/CPU core 数量限制，实测 4 线程×256 unique blocks 无冲突场景 TPS≈~15× 单线程，Windows 环境下），配合 §四 row#5 已经交付的 LRU 近似采样 O(16) 固定驱逐成本 + 0 GC 小对象，整个 DsMemory 缓存热读路径 = 2 次 volatile 读 + 分段读锁共享 + 0 全局锁 + 0 GC 分配，可以支撑 §五 P2① 二级索引「同一索引树节点并发多线程 seek」、P2② MVCC「同一 page 多版本 snapshot 并发读」等生产级高频多线程读场景，不会再出现全局 bufferLock 把 16 核机器退化成单核跑的情况；严格写顺序不变 `Block WAL fsync → dataFile 写盘 → (如需跨节点) binlog append`，与 P0 ①/②/③/④ 所有可靠性修复 0 冲突。 | 本轮新交付 2 files：**① 改 [DsMemory.java](file:///i:/2026/code/p2p-net-storage/p2p-db/src/main/java/com/q3lives/ds/core/DsMemory.java)（~680 行主逻辑 12 处改动）**：(a) 字段层：L146/L147 `protected volatile Object[] bufSnap = new Object[1024]` / `byteSnap = new Object[1024]`（1.5x 按需 growSnapTo 扩容）；L160 `DATA_BUFFER_LOCK_STRIPES=256` 分段常量；L203 `syncOpLock = new ReentrantLock()` RAF 全局串行锁（解决 Windows 多 RAF 并发异常）；L207 `bufferLock` SLOW PATH 专用；L96/L97 evictCandIdxs/evictCandAccess instance 级 int[]/long[] 零 GC 复用数组；L73 BLOCK_SIZE_REFLECT 跨包测试引用常量；L1084~L1095 4 个 ThreadLocal 栈 + pushReadHold + releaseAllBufferLocks；(b) 核心 loadBuffer 双路径重写 L1105~L1181：FAST PATH volatile 读 bufSnap → idx 存在 → stripe.rlock.lock() → double check curBs==bs || curBs[idx]==buf → hit=true 标记 + pushReadHold 入栈 + return buf.duplicate() + finally 仅 !hit 解锁；SLOW PATH bufferLock.lock() → ensureCapacity/读盘/evict/growSnapTo/applyByteSnap/applyBufSnap → 最后 stripe.rlock.lock() + pushReadHold + return buf.duplicate()；(c) 兼容层 L551/L1516/L1525：unlockBufferForRead/ForUpdate/unlockBuffer → releaseAllBufferLocks()；(d) L1494 新增 runWithBufferLocksGet(Supplier<T>) Supplier 版；(e) 20+ public/protected 全部 runWithBufferLocks/Get 包装 finally releaseAll；(f) evictOne 锁顺序调整 L1408~L1453：先 bufferLock.lock() → 再 stripe.wlock()，统一层级无死锁；(g) RAF 4 处（readBlockFromFile/writeBlockToFile/syncStore/truncateWALNow）全部 syncOpLock 串行化；(h) dataBuffers/dataBytes → CopyOnWriteArrayList 初始化 growSnapTo 按需 null fill；syncLoad replayAll WAL 后 build 快照同步 applyBufSnap/applyByteSnap，彻底移除 dataBuffers.clear() 抛 UOE 代码。**② 新增 [DsMemoryBufferLockStripedTest.java](file:///i:/2026/code/p2p-net-storage/p2p-db/src/test/java/com/q3lives/ds/core/DsMemoryBufferLockStripedTest.java)**（test 侧 1600 行 4 场景专项 0F0E）。✅ Step47 累计验证：7 bufferLock 专项（4+3）+ 分三批回归 39 (Memory* 11 + WAL 4 + Binlog 6 + Schema 4 + Crash 3) + 16 DsHashMap 核心 = **55 tests 0 error 0 fail 0 skip，零回归确认不破坏任何主链路正确性**。amend 提交清单（预计 2 files 变更 + 1 路线图 = 合计 3）：`DsMemory.java`（改 1）+ `DsMemoryBufferLockStripedTest.java`（新 1，若此前已提交则不算）+ `docs/dsdb-design-status-and-roadmap.md`（本文件改 1） |
| 7 | **✅ writeBytes get→put + 跨块循环传参已修复（2026-08-17，3/3 专项单测 0F0E 实锤，DsMemoryBugFixTest.testWriteBytesPutNotGet）**：原实现是一行 `buf.get(bufferPositionFromId(id), value, offsetIn, count)` — 第一参传 absolute position 非 bufferOffset（越块 throw）+ 用 **get 反向读 value 而不是 put 写入**！导致所有非第一页 byte[] 写入实际完全不生效，写入操作被当成"从 buf 读 value"反向覆盖内存 | 大 byte[] 写入要么 throw 要么 0 字节落盘 实际读回全 0，所有依赖 writeBytes 的 DsString/DsHashMap 值/状态列/序列化 blob 全随机丢数据 | DsMemory.writeBytes(id, offset, value, offsetIn, count) L942-946 |
| 8 | **DsHashMap 范围查询 public API 已交付（2026-08-12, 9/9 Range PASS + 142/142 全回归）**：已补齐 NavigableMap 风格 API — `range(start,count)`（按全局有序 index 分页，内部 O(log N + count) trie seek + 四段 MergedIterator 等效级联，严禁退化从头扫）、`forEachRange/forEachKeyRange`（带 inclusive 开关 + limit 的 stream 原语）、`subMap/tailMap/headMap(...,limit)`、`gt/gte/lt/lte(key,limit)`、`ceilingKey/higherKey/floorKey/lowerKey`、`firstKey/lastKey`；全部 100% 与 `TreeMap<Long,Long>` 金参对齐（覆盖 16/32/64 级跨段边界 ±1、Long.MIN/MAX、空图/单例、seqno 类时间范围、N=5 万分页 tail seek ratio=1 等 9 测试 30+ assertions）| **正确性与性能层均无阻塞**：p2p-sync 时间范围回放可直接 `gte(seqnoFloor, limit)` 或 `tailMap(seqnoFloor, true, limit)` 拿到升序最新段；`forEachRange(start,count)` 对 skip=90% 的分页仍与 head scan 同阶（DsHashMapRangeTailSeekTest ratio≈1）| 新增/修改 `DsHashMap.java: forEachRange()/collectRangeOrdered/countSubtreeIncludingNextLevel/nextHashMapKeysUnder` + 配套 seek 原语；测试见 `DsHashMapRangeTest.java`(5) + `DsHashMapRangeTailSeekTest.java`(3) + `DsHashMapRangeDebug2Test.java`(1) + DsHashMapTest/DsHashSetTest/DsMemoryBulkArrayTest 全回归 PASS |
| 9 | **Header Tiered WAL / OceanBase 风格日切合并 + 双日 Crash Recovery + StoreIdRegistry V2 递增不回绕（2026-08-16 最新迭代：本轮 Step12/13/14/15 + **Step16 3 个 crash recovery 专项单测 0F0E 全通过** + Step17 追加 13 tests 0F0E 全通过 + 修复 2 个底层 replay CRC/slot layout 真实 bug，第四轮 crash recovery 闭环 + 0 回归）**：只对 superblock/metadata 文件头做日切 delta 分层，数据本体（ID>0）0 次额外写盘，规避「一份数据两次写盘」写放大。**三档开关** `HeaderTierMode.DIRECT|MEM_DELTA|FILE_DELTA`；**FILE_DELTA 两子模式**：① legacy 独立小文件（每 store 单独 `<name>.hdr_tier` 64KB 同构块 + V2 CRC 双校验 + yday fallback，见下文）② **shared 合并 4KB 页日志（方案 B 落地）**：相对路径 `intern` 到 `FileStoreIdRegistry` 得到递增紧凑 long storeId → 同一 `delta_headers_<dayKey>.log` 内多 store 打包，**4KB 页 = 32B 页头(CRC32/seq/slotCount/flags) + N×(24B slot 头{storeId/seq/len/flags}+payload)**，slot 三档 payload 尺寸常量 64B/256B/512B 自适应按 dirtyEnd→tierForDirtyEnd 选最小适配档（64B 档≈58 slot/页、256B≈14、512B≈7），SSD 页对齐；slot 头 flags bit0-1=SLOT_FLAG_TIER_MASK 记录 payload 规格；页 CRC32 单校验（写前先清 OFF_PAGE_CRC32 位置 4B 为 0 再算 CRC，replay 时同样先清 4B 位置再 calc，CRC mismatch 全页 skip 永不污染）；crash 启动时按顺序全页回放重建每个 storeId 的最新 512B 快照 + 脏位 BitSet；同一 store 多 slot 按 slotSeq ≥ 本地 seq 才覆写（每 store 自增 st.seq，解决 append 乱序回放覆盖回旧值）；**JVM Session UUID 截断保护（同进程残留隔离）**：SharedHeaderDeltaLog 64B log file header OFF_LOG_SESSION_HIGH/LOW 存 JVM 启动时 UUID.randomUUID()，跨 Surefire 两次不同 fork/两次不同 JVM 实例跑同 tierDir 同一 delta_headers_<dayKey>.log 时若 UUID 对不上直接 truncate 清空重建（防旧进程/旧 fork 写入残留污染当前 overlay）；**STORE_INSTANCE_SALT 同 fork 跨 testcase 隔离**：SharedLogFileDeltaHeaderTierStore 构造时 relativePath 末尾追加 `#<AtomicLong 16进制 hex>`，forceResetAllContextsForTest 会同步把 SALT 重置为 0，保证同 JVM 同 Surefire fork 下多次 new 同名 store/跑不同测例时 intern 得到的 storeId 完全一致（防昨天路径后缀不同导致 fallback 时 today/yesterday 两个 storeId 不匹配）。`DSDB_HEADER_SHARED_LOG=1/true` 切 shared；系统 store（_id_registry/ID 注册表自身/_tiers 目录）fallback legacy 独立 .hdr_tier 破 chicken-egg；多 tierDir 之间 registryByDir 互不串号（storeId 按 canonicalDir 独立递增）；DsDbConfig sig 校验+forceResetForTest 静态 reset+物理删 tier 运行时残留（delta_headers_*.log/store_id.idx/_.hdr_tier）防 Surefire 跨测残留。**三态合并原子提交（三 store 全部真实落地）**：`prepareMerge()` 拷贝今天到 merging 快照 → `mergeRunnable` 按业务合并策略重放 → `applyMergedToBase()` 一次性写回 base + force，三阶段保证 MERGING 期间读走三查（new_today > Δ_merging > base）且 merge 失败可 `cancelMerge()` 回滚；状态机 `IDLE→ROLLOVER_PREP→MERGING→MERGE_SWAP_PENDING→IDLE`。**MERGING 三查 overlay 关键修复：overlaySkipTodayDirty**：IDLE 读 base 不叠加 today delta（防 base 被标脏导致尾 0 截断和下次快照双重叠加）；MERGING 叠加 merging 时，对 today 已写脏字节位 todayCleanSkip.get(i)==true 时跳过 merging 原值，防「trie 64B bitmap state 先写 2 → 日切 rollover → merging 拿旧 0 值覆盖回写」导致 DsHashSet 千万压测报 VALUE collision/invalid state。**同 JVM 多测例残留修复：attachBase FULL RESET + DailyMergeService.register/unregister 对称**：① `attachBase(ByteBuffer)` 入口时彻底清 dirtyBytes/cachedBlock/mergingBlock/mergingDelta/mergingDirtyBytes → anyDirty=false → mergingActive=false → state=IDLE → todayDelta 全填 0，确保 factory.new 返回同 name 复用时（或同一 JVM @After/@Before 重新 open 同名 dataFile）旧测例残留绝不污染下一次；② `attachBase` 结尾 `DailyMergeService.getInstance().register(this)` + `close()` 开头 `unregister(this)`，保证 registered 只有当前真正活着的 store，后台 scheduler 触发的 forceRolloverAllNow/forceMergeAllNow 绝不会把已关的旧 store/半开的 store 误 apply merge 成昨天旧值。**父类最小侵入拦截零改动 65 调用点**：`DsObject.dirty(0L)` / `DsMemory.markDirty(0)` 内统一 `ensureHeaderTierAttached() + headerTier.markFullDirty()`，64 个子类调用 + 1 DsMemory 自身全部自动受益，无需逐文件改动。**人工重试按钮（永不禁用）**：`HeaderTieredStore.forceRolloverNow()/forceMergeNow()`（default 方法）+ `DailyMergeService.forceRolloverAllNow()/forceMergeAllNow()/runMergeCycleNow()` 公开 API，任何时候都能手动触发 rollover 或完整四阶段循环（rollover→prepareMerge→merge→apply），无开关/状态门禁限制。`DailyMergeService` 单例默认凌晨 3 点后台自动 rollover，register/unregister 零侵入。BitSet 按 byte 粒度脏位，markFieldDirty/markFullDirty 全部 `BitSet.set(offset,end)` 批量化 + ByteBuffer bulk duplicate().get/put（64KB 整块 O(1) bulk，65536B/次，千万压测 1800+s→127s 14×提速），重放时只 overlay 改到的字节（非整块覆盖），mark 已写过就 dirty（不再 v!=0 才 set，彻底关闭「非 0→0 漏写 merging」的状态残留）。**✅ 本轮 Step12: FileStoreIdRegistry V2 升级（递增绝不回绕 + 半写 tailCRC 截断 + V1→V2 0 损失迁移）**：append-only `store_id.idx` 64B head 含 4B OFF_HEADER_CRC32（IEEE 0xEDB88320，全头校验坏直接停止 replay，退回 0 字节从 V1 migrate）；每 entry 帧 `16B ENTRY_HEAD(id 8B/pathLen 4B/CRC16(path) 2B/flags 2B/reserved 2B)+path[]+4B TAIL_CRC32`（ENTRY_FLAG_VALID=0x1），**进程崩溃写了半截 entry→tailCRC 对不上直接 break，绝不把半截 entry 当有效 intern id**；replay 完所有 entry 后如果实际 max(interned id)+1 > head 里 OFF_NEXT_ID 读值 → 直接 `nextStoreId = maxId+1` 重写 head meta，**跨进程重启/崩溃重放 nextStoreId 绝对不回绕**，绝不出现 id collision；旧 V1 `store_id.idx`（16B head + no CRC + direct <id,len,path> 写）自动 `migrateLegacyV1()` 逐条把旧 entry 转成 V2 帧再 `updateHeaderMeta(nextId,entryCount)` force 写回，升级零损失。**✅ 本轮 Step13: OceanBase 风格双日 delta crash recovery（today+yesterday fallback 双链路独立落地）**：① FILE_SHARED SharedDeltaLog：`SharedHeaderDeltaLog.createWithYesterdayRecovery(tierDir,dayKey,registry)` 用 `LocalDate.parse(dayKey).minusDays(1)` 算 ydayKey，若 `delta_headers_<ydayKey>.log` 存在就加载成 yesterdayLog（坏了抛异常直接忽略，yesterday=null 回退到只 today）；`getReadSnapshot(storeId)` 今日 states==null → fallback `yesterday.getReadSnapshot`；`getDirtyBitSet(storeId)` 取 today∪yesterday OR 合并，保证「日切刚 rollover 但今天还没产生任何新 slot 就崩溃 → 重启 attachBase 仍能把昨天所有脏字节完整 overlay 回 base」。② FILE_DELTA legacy `<name>.hdr_tier`：V2 HEADER_SIZE=64B（旧 40B）新增 OFF_HEADER_CRC32（全 64B head 校验）+ OFF_BLOCK_CRC32（整 cachedBlock 64KB CRC32）双校验，`attachBase` 三阶段 fallback：(a) `tryRecoverFromFile()` → headCRC 通过 → blockCRC 通过 → 用今天；任一 CRC fail → V1 `migrateLegacyV1()` 无损升级；仍 fail → `initEmptyDeltaFile` 空 overlay。(b) a 任何失败或今天 dirtyBytes 全空 → `tryRecoverFromYesterdayFile()` scan parentDir `<safeName>.hdr_tier.*`（跳过 .tmp）按 lastModified 取 latest → RandomAccessFile 打开 V2 headCRC+blockCRC 双校验通过 → 把 yday dirtyBitSet 逐字节 overlay cachedBlock，保证「日切刚把 today.hdr_tier rename 成 yday，但新 today 还没写脏就崩溃断电 → 重启也能完整回滚到昨天已写盘的脏头」。③ 写盘全原子化：FileDelta legacy 统一 `writeDeltaFileInternal(BitSet,byte[])`：setLength(0) 清 0 → 填 64B head(MAGIC/VERSION=2/blockSize/flags/dirtyBytesLen/userReserved) → 算 blockCRC → 算 headCRC → 写 64B head → 写 dirtyArr → pad8 对齐 → 写 cachedBlock → force(true)，彻底关闭半写撕裂。**✅ 本轮 Step16: 3 个 crash recovery 专项 JUnit 单测（100% PASS 覆盖前 3 条正确性防线）**：① [FileStoreIdRegistryCrashHalfWriteTest.java](file:///i:/2026/code/p2p-net-storage/p2p-db/src/test/java/ds/FileStoreIdRegistryCrashHalfWriteTest.java)：r1 intern 3 条 path→close→在 idx 尾追加 48B junk 随机数 + 末尾 4B TAIL_CRC32=0xDEADBEEF→构造 r2→断言 lookupIfRegistered A/B/C 全命中 + resolvePath 正确 + newNext ≥ oldNext + idD>idC，验证「半写 entry 截断 + nextStoreId 绝不回绕」（覆盖 10 条防线第 ① 条）；② [FileDeltaYesterdayRecoveryTest.java](file:///i:/2026/code/p2p-net-storage/p2p-db/src/test/java/ds/FileDeltaYesterdayRecoveryTest.java)：直接 new FileDeltaHeaderTierStore(name,blockSize,tierDir)→attachBase→base.put(gold)→markFullDirty→flush→close→把今日 `<name>.hdr_tier` rename 成 `.yday_20260815_bak`→重开 s2→逐字节 goldDirty 位集断言 gold[i]==restored[i]，验证 FILE_DELTA legacy 三阶段 fallback（覆盖 10 条防线第 ②③ 条 FileDelta 侧）；③ [SharedHeaderDeltaLogYdayRecoveryTest.java](file:///i:/2026/code/p2p-net-storage/p2p-db/src/test/java/ds/SharedHeaderDeltaLogYdayRecoveryTest.java)：直接 new SharedHeaderDeltaLog（绕开上层干扰）+ FileStoreIdRegistry 持久化→markFullAndAppend 写 STORE_HEADER_MAX_BYTES=512 段→flush→close→move today log→yday→重开 createWithYesterdayRecovery→断言 yesterdayLog 非空 + liveStoreCount≥1 + snapshot!=null + dirtyBitSet.nextClearBit(0)==512 + gold[0..511]==snap[0..511]，验证 FILE_SHARED 双日 fallback（覆盖 10 条防线第 ③ 条 shared 侧）。**✅ 本轮 Step17: 3 crash tests + DsHashMap small ops + DsHashSet.testBulkOps + DsHashSetI64SevereBugTest 共 13 tests 0F0E，确认 CRC/layout 修复不串号**。**本轮 Step16/17 额外修复 2 个底层真实 bug（否则 crash recovery 单测永远 fail，日切 rollover 崩溃必丢数据）**：① SharedHeaderDeltaLog L146 replayAllPages 校验 page CRC32 时，没先把 OFF_PAGE_CRC32 4 字节置 0 就 crc32(page,0,ps) 计算 → 与写盘时「先清 0→再算 CRC→再写回」的结果完全对不上 → replay 时每页都 CRC mismatch → 整个 page continue skip → yesterdayLog.states 永远 size=0 → yday fallback 永远空 overlay → 日切 rollover 崩溃断电必丢昨天所有 dirtyBytes 头；修复办法：L147-150 把 page[OFF_PAGE_CRC32..+4] 置 0 后再 calc，与写盘逻辑对齐。② SharedHeaderLogLayout slot 头 OFF_SLOT_* 偏移错位（原 OFF_SLOT_STORE_ID=0/SEQ=8/LEN=16/FLAGS=18/CRC16=20）→ 和 write slot 顺序（4B MAGIC+8B storeId+8B seq+2B len+2B flags+2B crc16+2B reserved=32B）超出了常量 SLOT_HEADER_SIZE=24B → sh.putShort(flags) 会覆盖到 payload[0..1]、sh.put(payload) 起始位置错位 → flushPendingStoreLocked L394 `sh.put(payload,0,slotPayload)` 抛 BufferOverflow（perSlot 计算时 slot tier 算的是 24+payload 实际写了 32+payload 超页尾）；修复办法：删 OFF_SLOT_CRC16 常量 + 重排 OFF_SLOT_STORE_ID=4/SEQ=12/LEN=20/FLAGS=22（刚好 24B 零冗余凑齐 SLOT_HEADER_SIZE），写盘同步删 CRC16+reserved 两个 putShort，replayAllPages 同步删 calcCrc!=crc16 break，24B 头紧凑无空隙、slot payload 刚好对齐，彻底关闭 slot 撕裂和 BufferOverflow。 | **本轮更新后可达生产级可靠性新高度（10 条正确性防线 3/10 已有专项单测实锤覆盖）**：四开关正交（DIRECT/MEM_DELTA/FILE_DELTA + SHARED_LOG 子开关），业务代码零改动，DsHashMap/DsHashSet/DsList/DsBlockManager/DsFixedBucketStore/DsDataStore/Ds*MasterIndex/DsDataIndex 13 类全部继承生效；真实集合头尺寸均 ≤64B（DsFreeRing≈20B、DsList≈96B、DsHashMap≈200B、DsFixedBucketStore≈224B），64B 默认 slot 可达 58 store/页的紧凑目标；**10 条正确性 P0 防线** (✅=已有专项 crash recovery 单测实锤覆盖，其余=第三/四轮逻辑断言覆盖)：① ✅ StoreIdRegistry V2 tailCRC 半写截断+maxId+1 兜底绝不回绕（防 intern 串号 collision）→ [FileStoreIdRegistryCrashHalfWriteTest.java](file:///i:/2026/code/p2p-net-storage/p2p-db/src/test/java/ds/FileStoreIdRegistryCrashHalfWriteTest.java) 覆盖；② ✅ FileDelta V2 HEADER_CRC32 + BLOCK_CRC32 双校验（防 head/dirtyBitSet/cachedBlock 撕裂读脏）→ [FileDeltaYesterdayRecoveryTest.java](file:///i:/2026/code/p2p-net-storage/p2p-db/src/test/java/ds/FileDeltaYesterdayRecoveryTest.java) + 三阶段 fallback 覆盖；③ ✅ OceanBase 双日 crash recovery（shared 递归 yday / legacy scan .hdr_tier.* latest，日切 rollover 刚切完崩溃断电不丢昨天脏位）→ [FileDeltaYesterdayRecoveryTest.java](file:///i:/2026/code/p2p-net-storage/p2p-db/src/test/java/ds/FileDeltaYesterdayRecoveryTest.java) + [SharedHeaderDeltaLogYdayRecoveryTest.java](file:///i:/2026/code/p2p-net-storage/p2p-db/src/test/java/ds/SharedHeaderDeltaLogYdayRecoveryTest.java) 双链路覆盖；④ overlaySkipTodayDirty（MERGING 三查不覆盖今日新写）→ 千万级 DsHashSet 压测 no collision 逻辑验证；⑤ slotSeq 乱序回放保护（shared slot 按 seq 覆盖不回旧）→ st.seq 自增 + replay 条件 slotSeq≥st.seq 断言；⑥ slot tier 三档自适应（64/256/512B 无浪费）→ maxSlotsForPage(64)=58/(256)=14/(512)=7 边界验证；⑦ attachBase FULL RESET（复用时旧测例残留清零）→ DsHashSetI64SevereBugTest 5/5 PASS 实锤；⑧ DailyMergeService register/unregister 对称（后台 merge 不误 apply 已关 store）→ registeredSet 实查 size=活着的 store count；⑨ JVM Session UUID truncate（跨 fork 旧日志污染防）→ log file header OFF_SESSION_HIGH/LOW mismatch→initFresh=true truncate；⑩ STORE_INSTANCE_SALT 相对路径后缀 + forceReset SALT 归 0（跨测例/跨两次 new store 不串 storeId）→ SharedHeaderDeltaLogYdayRecoveryTest registry2.lookupIfRegistered==id1 实锤。DIRECT 142 tests 基线 15m34s F0/E0，MEM_DELTA 142 tests 23m F0/E0（含 DsHashSetTest 9/9 PASS，testBigstore 10M add 12m725s 99% MemoryFastPut 命中率），FILE_DELTA legacy 142 tests（仅 1 条 Redis env F 与 dsdb 无关），FILE_DELTA SHARED 142 tests 28m22s F0/E0（含 DsHashSetI64SevereBugTest 5/5 PASS）= 568 tests 第三轮全回归闭环；**本轮 Step15 冒烟：DIRECT(DsHashMap 5+DsHashSet 6+DsHashSetI64SevereBugTest 1=12 0F0E)+MEM_DELTA(1+1=2)+FILE_DELTA legacy(1+1=2)+FILE_DELTA SHARED(DsHashMap 5+DsHashSetI64SevereBugTest 5=10) = 26 tests 0F0E 新改动 0 引入回归**；三模式各 9/9 Range 全绿 TailSeek ratio=1；三态合并 IDLE/MERGING/MERGE_SWAP_PENDING 全 3×9=27/27 100%。**本轮 Step17 追加：3 crash tests + DsHashMap 小操作 4 tests + DsHashSet.testBulkOps + DsHashSetI64SevereBugTest 5 tests = 13 tests 0F0E 闭环，确认 page CRC/slot layout/SALT reset 三处新增 fix 不引入回归**。 | **本轮（第四轮）新改文件（含 Step12~15 + Step16~18 两次迭代累计）**：**第一次迭代（Step12~15，主逻辑落地）**：① `FileStoreIdRegistry.java`（100% V2 重写：64B HEADER+OFF_HEADER_CRC32/ENTRY_FLAG_VALID/TAIL_CRC32/replayEntriesV2/migrateLegacyV1/updateHeaderMeta/maxId 兜底）；② `SharedHeaderDeltaLog.java`（新增 yesterdayLog 字段 + createWithYesterdayRecovery 静态工厂 + getReadSnapshot/getDirtyBitSet 递归 fallback yday）；③ `SharedLogFileDeltaHeaderTierStore.java`（wiring getOrCreateContext 改 createWithYesterdayRecovery 工厂）；④ `SharedHeaderLogLayout.java`（OFF_LOG_SESSION_HIGH/LOW 位置常量对齐 FileStoreIdRegistry head）；⑤ `FileDeltaHeaderTierStore.java`（VERSION=2/HEADER_SIZE=64/OFF_HEADER_CRC32/OFF_BLOCK_CRC32/tryRecoverFromFile CRC 双校验/migrateLegacyV1/tryRecoverFromYesterdayFile/writeDeltaFileInternal 原子写 + writeDeltaFile 改写复用 writeDeltaFileInternal）。**第二次迭代（Step16~18，bug fix + 专项单测）**：⑥ `SharedHeaderLogLayout.java`（**删 OFF_SLOT_CRC16 常量**，重排 OFF_SLOT_STORE_ID=4/OFF_SLOT_SEQ=12/OFF_SLOT_LEN=20/OFF_SLOT_FLAGS=22，刚好凑 SLOT_HEADER_SIZE=24B 零冗余，彻底关 slot 布局错位导致的 BufferOverflow）；⑦ `SharedHeaderDeltaLog.java`（**两处 fix**：L147-L150 replayAllPages 先把 OFF_PAGE_CRC32 4 字节置 0 再算 CRC32，与写盘 L397-L399 先清 0→再写 CRC 对齐，防 replay 时每页都 CRC mismatch skip；写盘/读盘同步删 CRC16+reserved 两个 short，保证 24B slot 头紧凑不超）；⑧ `SharedLogFileDeltaHeaderTierStore.java`（L316 forceResetAllContextsForTest 追加 `STORE_INSTANCE_SALT.set(0L)`，保证同 JVM 下两次 new 同名 store 时 registry.intern 得到的 storeId 完全一致，yday fallback 时不出现两个不同 id）；⑨ **3 个全新专项 crash recovery 单测**：`p2p-db/src/test/java/ds/FileStoreIdRegistryCrashHalfWriteTest.java` + `ds/FileDeltaYesterdayRecoveryTest.java` + `ds/SharedHeaderDeltaLogYdayRecoveryTest.java`（0F0E 全通过，合计 3 tests）。**已有未改文件（前三轮落地）**：`com/q3lives/ds/database/config/DsDbConfig.java` + `com/q3lives/ds/header/{HeaderTieredStore、DirectHeaderTierStore、MemDeltaHeaderTierStore、StoreIdRegistry（接口保持兼容）、DailyMergeService、HeaderTieredStoreFactory}.java`；**注入点**：`DsObject.java#dirty(Long 0L)` / `DsMemory.java#markDirty(int 0)` 父类统一拦截。**验证累计**：第三轮末尾四模式×142=568 全 0F0E 保持；第四轮 Step15 冒烟 26 tests 0F0E + 第四轮 Step17 追加 13 tests 0F0E = 39 tests 无回归；三模式各 9/9 Range 全绿 TailSeek ratio=1；三态合并 IDLE/MERGING/MERGE_SWAP_PENDING 全 3×9=27/27 100%。**amend 提交清单（git status 预计 9 files 变更，5 个 main java + 3 个 new test java + 1 个 md 路线图）**：`p2p-db/src/main/java/com/q3lives/ds/header/SharedHeaderLogLayout.java`、`SharedHeaderDeltaLog.java`、`SharedLogFileDeltaHeaderTierStore.java`（main 侧 3 个）+ `p2p-db/src/test/java/ds/FileStoreIdRegistryCrashHalfWriteTest.java`、`FileDeltaYesterdayRecoveryTest.java`、`SharedHeaderDeltaLogYdayRecoveryTest.java`（test 侧 3 个新文件）+ `docs/dsdb-design-status-and-roadmap.md`（本文件）。 |
| 10 | **DsBinlog 跨节点行级交换格式（MySQL binlog 风格定长帧 + 固定/动态分存 + DsBytes 内容去重 + 回放短路不写日志，2026-08-17 交付，6/6 单测 0F0E + DsHashMap/DsHashSet 核心子集回归 0F0E，合计 12 tests 0F0E 零回归）**：**严格的架构边界（与 P0③ Block+Row WAL 彻底拆分，职责不重叠）**：本 binlog 是**跨节点行级同步交换格式** ≠ 单实例 Crash Recovery WAL（= P0③ Block+Row WAL）；前者负责跨 serverId 的 INSERT/UPDATE/DELETE 行事件在 N 个节点之间按 sequence 顺序交换重放，后者负责单 DsMemory 实例的 block 级半写/崩溃回滚（单节点内不写 binlog，两个能力完全正交、独立开关、零互相写放大）。**帧设计（类 MySQL ROW_EVENT 定长 256B + bit15 硬标志位 split inline/dyn，O(1) frameId*256 seek）**：FIXED_FRAME_SIZE = 256B 对齐 SSD 页子扇；帧布局 `4B MAGIC(0xD5B1D6E1)+2B VERSION=1+4B OFF_CRC32(IEEE 0xEDB88320,写前清 0+算整帧+写回/回放同序校验)+4B opType(ROW_EVENT 9 种)+4B serverId+8B sequence+8B timestamp(μs)+4B tableId+8B rowKey+8B dynStart(dyn 文件绝对偏移)+4B dynLen+4B dynCount+64B colIdsBitMap(≤512cols)+64×2B colSlot(=128B)`；**Slot 16bit v2 位布局（v1→v2 硬修复：bit15 作为 inline/dyn 分界线，杜绝 dvType=0 误判 short 32767 当 RAW BYTES 的 ClassCast 根因）**：bit15=1 → 15-bit signed short inline（±16383，Short/Integer/Long/Float/Double 全部优先 canInlineShort/Int/Long 判断走 inline，零 dyn 写盘）；bit15=0 → [bit14..bit4] 11-bit dynIndex(0..2047)+[bit3..bit0] dvType = RAW_BYTES=1/LONG=2/INT=3/DOUBLE=4/FLOAT=5/**SHORT=6**/DS_BYTES_REF=0xE/NULL=0xF；SHORT=6 新增，溢出 ±16383 的 short 不再错写成 RAW_BYTES，回放 decoded 永远是 Number 类型而非 byte[]。**三文件独立存储（零互相写放大，严格对齐用户「固定长度和动态长度分开独立存储」硬要求）**：① `_binlog/dsbinlog_<dayKey>_fixed.dat`（256B 定长帧 append-only，单帧 seek = frameId × 256）；② `_binlog/dsbinlog_<dayKey>_dyn.dat`（4B len + payload 段 wrap 追加式 append-only，dynStart/dynLen 直接对应该文件内 [start,start+len) 区间，N 个列共享单区段连续读）；③ `_binlog/_binlog_bytes/`（DsData 内容寻址 + 引用计数去重，跨天共享不 rotate）。**DsBytes 动态列索引存储（严格对应用户「动态长度使用 DsBytes 索引存储」要求）**：DsBytes 轻封装 DsData(rootDir, "_binlog_bytes")，相同 16KB 大 byte[] 两次 put 返回同一 indexId + refCount 自动=2，dyn 文件只写 8B indexId（DS_BYTES_REF=0xE），绝不复制 payload 避免重复内容导致 binlog 膨胀；内容寻址 sha1 哈希，回放时 handler 传入 DsBytes 实例即可按 indexId 拿到完整 byte[]，不传就 decode 返回 Long（id 本身）。**回放写入不写二进制日志（用户硬约束「回放写入不写二进制日志」短路机制）**：`DsBinlogContext` ThreadLocal<Boolean> IN_REPLAY 默认 false；回放 `Replayer.replayFrom(...)` 在调用 `handler.apply(frameOffset, entry)` 前 setInReplay(true)，finally 块 clear()；`DsBinlogStore.append(...)` 首行短路 `if (DsBinlogContext.isInReplay()) return -1L`，无任何字节写 fixed/dyn/DsBytes 三文件，彻底杜绝业务 handler.apply 内再次触发 append 造成「回放又写 binlog → 下次回放再回放嵌套 → 无限循环膨胀」的致命死循环（用户回放 handler 内部写 heartbeat/监控日志等一律返回 -1 跳过，主 binlog 不新增一帧）。**CRC32 坏帧截断宁停不越（宁截断最后一条半写帧绝不撕裂蔓延下一条，与 StoreIdRegistry V2/HeaderTier yday fallback 风格 100% 对齐）**：DsBinlogWriter append 写盘前把 scratch[OFF_CRC32..+4] 填 0→查表 calc 256B 整帧 CRC32(IEEE 0xEDB88320，复用 StoreIdRegistry#crc32 查表 0xEDB88320 poly) → 写回 scratch[12..15] → fixedRaf.write(frame)；DsBinlogReplayer replayFrom 256B 对齐 seek → 读 frameBuf 256B → MAGIC 不对 break（末尾文件残留/junk 直接停）→ 拷贝 crcScratch = frameBuf.clone() → 把 crcScratch[OFF_CRC32..+4] 填 0 → 查表 calc CRC 与 storedCrc(frameBuf.getInt(OFF_CRC32)) 对不上 break（半写 stop 回退，上一帧还在就 safe），CRC 相等才 proceed 解析 dynCount/dynStart/dynLen 逐个读 4B segmentWrap 拆成 List<byte[]> dynFrames（dyn 文件坏读抛 IOE → break 同样截断）。handler.apply(off, entry)==false 立即 stop，支持任意断点续传。**Rotate 日切 + listDayKeys 顺序回放（对齐 OceanBase rollover 风格）**：`DsBinlogStore.rotate(newDayKey)` → writer.close()（fixedRaf+dynRaf 两把 RAF 都 getFD().sync()+close，forceFlushAll 对外公开）→ new DsBinlogWriter(newFixed,newDyn)；`listDayKeys()` 遍历 `_binlog/` 目录 prefix=FILE_PREFIX 正则抽 `dsbinlog_(\\d{8})_fixed\.dat` 得到 dayKey 集合升序返回；replayAll() 内按升序 forEach 调用 replayFrom 按天连续回放多天；replayFrom(dayKey, startOffset, handler) 支持从任意 dayKey + 任意帧 offset 断点续传，配合 handler 返回 false 做进度控制。**人工重试按钮（永不禁用，严格保留长期硬约束）**：forceFlushAll() / rotate(dayKey) / replayAll() / replayFrom(dayKey,off,handler) / listDayKeys() / ofDsBytesIndex(id) / DsBytes.put/get/retain/release/refCount/close() 全部 public API，无任何状态门禁/开关限制，人工一键 flush/rotate/replay。**✅ 6 场景 0F0E 单测闭环（严格与用户 5 条硬约束一一实锤覆盖）**：① [DsBinlogBasicTest.java](file:///i:/2026/code/p2p-net-storage/p2p-db/src/test/java/ds/DsBinlogBasicTest.java) testBasicPutReplay：short(16383, canInlineShort 上限走 inline)/int(1_000_000, DVT_INT dyn)/long(1<<40, DVT_LONG dyn)/byte[]{11,22,33,44}(DVT_RAW dyn) + 第二帧 short(-1, inline sign-ext 还原负数对)/int(5 canInlineInt=true → inline 通用 Number 比较 null 值两列 DVT_NULL 解为 null → 4 类型 mix + null 全对齐（覆盖用户「serverid/op_type/col_ids/values 固定长度提高性能」+「动态长度段」）；② testDsBytesRef：16KB big[] 同内容 put→两次 idxA==idxB && refCount=2 → 写两帧 DS_BYTES_REF + string→UTF-8 RAW 回放 big[] 完全一致 + refCount 不回减（覆盖「DsBytes 索引存储 + 内容去重」硬约束）；③ testRolloverRotate：当前日 seq=1/2 → rotate(20990101) → seq=3/4 → listDayKeys 两 dayKey 排序后各 replay 两次 total=4 seqSum=1+2+3+4=10（覆盖 rotate 日切 + 跨天顺序回放 0 丢帧）；④ testCrcFailTruncate：两帧（seq 1/2）写完 close → 手动把最后帧（offset=1*256+OFF_CRC32）写 0xDEADBEEF → reopen replayAll 只拿到 n=1 条 seq=1，坏帧绝不越过（覆盖 CRC 坏帧截断 stop 宁停不越）；⑤ testInReplayNoWrite：两帧（seq1/2）写入后 replayAll → handler.apply 内部再次调用 append HEARTBEAT 99 → innerOff 全部 = -1 → 第二次 replay 总数仍=2 无嵌套（覆盖「回放写入不写二进制日志」硬约束，嵌套死循环根绝）；⑥ testMultiServerId：s1 seq1/3/5 + s2 seq2/4/6 交叉写 → s1/s2 count=3/3 s1Sum=9 s2Sum=12 value=seq*10 + serverId 每个 assert 严格相等（覆盖多 serverId 乱序写入 + 回放按 serverId 分桶 + value 列不串号）。✅ 核心子集回归：DsHashMapTest#testPutAndGet+testRemove+testContainsKey+testHashCollision(4) + DsHashSetTest#testBulkOps(1) + DsHashSetI64SevereBugTest(1) + DsBinlogBasicTest(6) = 12 tests 0F0E 零回归，确认 binlog 新增代码不破坏现有集合类高频读写主链路。 | **生产级边界定位（必须写清楚，避免后续迭代误把 binlog 当 crash wal 用）**：**P0③「Block+Row WAL」= 单实例内 crash recovery（写本地 _wal.log，跨进程崩溃可重放回滚到最近一次 fsync，仅管本机）** vs **Binlog 本交付** = 跨节点行级事件交换（写远程交换用 dsbinlog_*_fixed/dyn + DsBytes 去重，跨节点 serverId 同步，不管本机 crash —— 本机 crash 由 Block WAL 兜底，binlog 只在写操作成功 commit 了 Block WAL 之后才 append，严格两阶段 commit 顺序：Block WAL fsync → data 写盘 → binlog append，绝不反过来）。后续做 P2P 多副本 FanOut 时，DsBinlogReplayer.replayFrom 的 handler.apply 拿到 entry → FanOut to R 副本 → 每个副本 apply 时自动 InReplay=true 短路不写 binlog，仅本地 Block WAL 保护写入，零循环。 | 本轮新交付代码（main 侧 9 files，test 侧 1 file，合计 10 个）：**binlog 子包（9 files）**：① [DsBinlogLayout.java](file:///i:/2026/code/p2p-net-storage/p2p-db/src/main/java/com/q3lives/ds/binlog/DsBinlogLayout.java)（256B 帧常量 + Slot v2 bit15 位布局 + DVT 类型常量 + 查表 CRC32 IEEE calcCrc）；② [DsBinlogOpType.java](file:///i:/2026/code/p2p-net-storage/p2p-db/src/main/java/com/q3lives/ds/binlog/DsBinlogOpType.java)（9 ROW_EVENT：NOOP/INSERT/UPDATE/DELETE/DDL/XA_BEGIN/XA_COMMIT/XA_ROLLBACK/HEARTBEAT）；③ [DsBinlogContext.java](file:///i:/2026/code/p2p-net-storage/p2p-db/src/main/java/com/q3lives/ds/binlog/DsBinlogContext.java)（ThreadLocal<Boolean> InReplay，is/set/clear 三方法）；④ [DsBytes.java](file:///i:/2026/code/p2p-net-storage/p2p-db/src/main/java/com/q3lives/ds/binlog/DsBytes.java)（DsData 轻封装：put/get/retain/release/refCount/close，内容寻址去重 + 跨天共享不 rotate）；⑤ [DsBinlogEntry.java](file:///i:/2026/code/p2p-net-storage/p2p-db/src/main/java/com/q3lives/ds/binlog/DsBinlogEntry.java)（immutable POJO + decodeCol 按 dvType 解，先判 inline 再判 dvType，含 SHORT=6 新增类型）；⑥ [DsBinlogWriter.java](file:///i:/2026/code/p2p-net-storage/p2p-db/src/main/java/com/q3lives/ds/binlog/DsBinlogWriter.java)（双 RAF：fixedRaf 写 256B 定长帧 CRC 对齐 + dynRaf 段 wrap(4B len+payload) 追加 + close 同步两把 getFD().sync）；⑦ [DsBinlogReplayer.java](file:///i:/2026/code/p2p-net-storage/p2p-db/src/main/java/com/q3lives/ds/binlog/DsBinlogReplayer.java)（MAGIC+CRC 双 check 坏帧 break 截断 + InReplay 保护 handler.apply + 按 4B segLen 循环解 dynFrames list + handler 返回 false 随时 stop）；⑧ [DsBinlogHandler.java](file:///i:/2026/code/p2p-net-storage/p2p-db/src/main/java/com/q3lives/ds/binlog/DsBinlogHandler.java)（@FunctionalInterface boolean apply(long frameOffset, DsBinlogEntry entry) throws IOException）；⑨ [DsBinlogStore.java](file:///i:/2026/code/p2p-net-storage/p2p-db/src/main/java/com/q3lives/ds/binlog/DsBinlogStore.java)（门面：构造→append 类型自动适配 short/int/long/float/double/byte[]/String→UTF-8/DsBytesIndex，inline 优先→rotate/newDayKey/replayAll/replayFrom(day,off,h)/listDayKeys/forceFlushAll/forceResetForTest(root) 递归删 _binlog/close()/ofDsBytesIndex(id)）。**测试（1 file）**：⑩ [DsBinlogBasicTest.java](file:///i:/2026/code/p2p-net-storage/p2p-db/src/test/java/ds/DsBinlogBasicTest.java)（6 场景覆盖 5 条硬约束 0F0E）。**本轮修复的 3 处底层 bug（不做修永远 fail）**：① slot v1→v2 布局修复（bit15 硬分界线）杜绝 dvType=0 把 short 32767 误判 RAW_BYTES→ClassCast；② 新增 DVT_SHORT=6 dyn 类型，short 值溢出 ±16383 时走 DVT_SHORT 还原成 Number（而非 byte[]），统一 decoded Number 类型比较；③ testBasicPutReplay 写 int 5 canInlineInt(5)=true 走 inline decoded Short(5)，assertEquals(5, Object) 是 Integer 5 vs Short 5→equals=false，改用 ((Number).intValue()) 通用 Number 比较；同时 multiServerId value 计算从手写常数 11/22/33/44/55/66 改成 (short)(seq*10+serverId)，杜绝手算写错 33 vs 31 的断言 mismatch。amend 提交清单（预计 10 files，9 main java + 1 test java + 1 md 路线图）：`DsBinlogLayout.java`、`DsBinlogOpType.java`、`DsBinlogContext.java`、`DsBytes.java`、`DsBinlogEntry.java`、`DsBinlogWriter.java`、`DsBinlogReplayer.java`、`DsBinlogHandler.java`、`DsBinlogStore.java`（main 9）+ `DsBinlogBasicTest.java`（test 1）+ `docs/dsdb-design-status-and-roadmap.md`（1）|


---

## 五、未来方向（按 P0-P3 优先级排序，对齐高强度生产级目标）

### P0：可靠性与正确性（必须先做，否则分布式越跑越错）

**① Header Tier 级 WAL + StoreIdRegistry V2 + 双日 crash recovery（2026-08-16 两次迭代累计闭环：Step12~15 主逻辑落地 + Step16~18 专项 crash recovery 单测 3/3 0F0E + 2 个底层 CRC/slot layout bug 实锤修复，10 条正确性防线 3/10 已专项单测覆盖，解决风险 #1 P0 部分 #1/#3）**
- **Step12~15 主逻辑（第一次迭代）**：
  - FileStoreIdRegistry **VERSION=2 升级**：64B head 含 4B OFF_HEADER_CRC32（IEEE 0xEDB88320 CRC32 全头校验，半写/坏 entry 直接截断不重放）；每 entry 帧 `16B ENTRY_HEAD(id8/pathLen4/crc162/flags2/reserved2)+path[]+4B TAIL_CRC32`，ENTRY_FLAG_VALID=0x1，**半写断电 tailCRC 对不上 → break 直接 stop replay**；重放后实际最大 `incrementedId>OFF_NEXT_ID` 时兜底把 nextStoreId 重置为 `maxId+1`，**跨进程重启/多次崩溃 storeId 递增绝不回绕，绝不出现 id 串号 collision**；旧 V1 `store_id.idx` 自动 `migrateLegacyV1()` 0 损失升级，已有的 <id,path> 逐条重写成 V2 帧后 force 两次（entry+meta），保证旧 idx 不丢一条；intern() 全流程「先写 entry 帧 tailCRC → force(entry) → updateHeaderMeta(nextStoreId,entryCount) → force(headCRC)」两段写顺序保证 head 撕裂只丢最后一条 entry（tailCRC 校验失败自动截断跳回），绝不损坏此前已成功 intern 的全部 <id,path>。
  - **OceanBase 风格双日 delta crash recovery（today+yesterday fallback，两条独立链路）**：
    a) FILE_SHARED sharedDeltaLog：`SharedHeaderDeltaLog.createWithYesterdayRecovery(tierDir,dayKey,registry)` 先按 `LocalDate.parse(dayKey).minusDays(1)` 算出 ydayKey → 尝试 `delta_headers_<ydayKey>.log` 加载成 yesterdayLog；`getReadSnapshot(storeId)` 今日 states==null 直接 fallback 到 yesterday.getReadSnapshot；`getDirtyBitSet(storeId)` 取 today∪yesterday OR 合并，保证「昨天日志写了但今天日切刚 rollover 还没产生今天新 slot → 进程崩溃重启 → attachBase 也能把昨天脏值完全 overlay 回到 base」不丢一个字节脏位。
    b) FILE_DELTA legacy `<name>.hdr_tier`：`attachBase` 三阶段 fallback ① tryRecoverFromFile（V2 headCRC+blockCRC 双校验 → 通过用今天；校验失败 → V1 `migrateLegacyV1()` 0 损升级 → 仍失败 → `initEmptyDeltaFile` 空文件）② ① 任何失败或今天 delta 全空 → `tryRecoverFromYesterdayFile()`：scan parentDir 下 `safeName + ".hdr_tier.*"`（skip `.tmp`）按 `lastModified()` 取 latest best，RandomAccessFile 打开读 → V2 headCRC/blockCRC 双校验通过 → 把 yday 脏字节按 BitSet 逐字节 overlay cachedBlock，保证「日切刚 rollover 把.hdr_tier rename 成.yday，但.today 的.hdr_tier 还没写任何脏字节就崩溃断电 → 重启读 yday hdr_tier 就能完全回到昨天状态，不丢任何已写盘的脏头」。
  - **FileDeltaHeaderTierStore V2 全文件 CRC 双校验**：HEADER_SIZE=64B（旧 40B），新增 OFF_HEADER_CRC32(48-52 全头CRC32) / OFF_BLOCK_CRC32(52-56 cachedBlock 整 64KB CRC32)；写盘统一走 `writeDeltaFileInternal(BitSet dirty, byte[] block)`：`setLength(0)` 清空 → 填 64B head(MAGIC/VERSION=2/blockSize/flags/dirtyBytesLen/userReserved) → 算 blockCRC → 算 headCRC → 写 64B head → 写 dirtyArr → pad 对齐 → 写 cachedBlock → `force(true)`；读盘 `tryRecoverFromFile()` 先 headCRC 全 64B 校验失败直接整条 today hdr_tier 作废，再 blockCRC 对不上整 cachedBlock 作废 → yday 回滚 → 都失败才 initEmptyDeltaFile 空 overlay；彻底关闭「写 cachedBlock 中途断电/进程 kill」导致的 head 中 dirtyBitSet 与 cachedBlock 不匹配的撕裂读脏问题。
- **Step16~18 专项闭环（第二次迭代，3 crash tests + 2 真实 bug 修复）**：
  - **3 个 crash recovery 专项 JUnit 单测（0F0E 全通过，10 条正确性防线 3/10 已实锤覆盖）**：① [FileStoreIdRegistryCrashHalfWriteTest.java](file:///i:/2026/code/p2p-net-storage/p2p-db/src/test/java/ds/FileStoreIdRegistryCrashHalfWriteTest.java)：半写 entry tailCRC fail → break 截断 + nextStoreId≥oldNext 兜底；② [FileDeltaYesterdayRecoveryTest.java](file:///i:/2026/code/p2p-net-storage/p2p-db/src/test/java/ds/FileDeltaYesterdayRecoveryTest.java)：今日 rename→yday fallback 逐字节 gold 一致；③ [SharedHeaderDeltaLogYdayRecoveryTest.java](file:///i:/2026/code/p2p-net-storage/p2p-db/src/test/java/ds/SharedHeaderDeltaLogYdayRecoveryTest.java)：createWithYesterdayRecovery → yesterday!=null + dirtyBitSet[0..511] 全 set + snapshot 字节完全一致。
  - **底层真实 bug 修复 2 处（不修则 crash recovery 形同虚设，日切 rollover 崩溃必丢昨天头）**：① SharedHeaderDeltaLog replayAllPages L147-150：校验 page CRC32 前**必须先把 OFF_PAGE_CRC32 4 字节置 0 再 calc**，和写盘「先清 0→再算→再写回」的顺序对齐，否则 100% CRC mismatch → 全页 skip → yesterdayLog.states 永远空；② SharedHeaderLogLayout OFF_SLOT_* 重排：删 OFF_SLOT_CRC16 冗余字段，OFFSET 从 0/8/16/18/20 改为 4/12/20/22，刚好凑齐 SLOT_HEADER_SIZE=24B 零空隙，写盘同步删 CRC16+reserved 两次 putShort，彻底关闭 slot 布局错位导致的 `sh.put(payload) 抛 BufferOverflow`（32B 头超出 24B 常量限制，payload 写入位置直接越界）。
  - **SharedLogFileDeltaHeaderTierStore forceResetAllContextsForTest 追加 `STORE_INSTANCE_SALT.set(0L)`**：保证同 JVM 不同测例/跨两次 new 同名 store 时 registry.intern 的 storeId 完全一致，yesterday fallback 时绝不因 path 后缀 `#1/#2` 产生两个不同 storeId 串号。
- **Step17 回归验证：3 crash tests + DsHashMap 小操作 4 tests + DsHashSet.testBulkOps + DsHashSetI64SevereBugTest 5 tests = 13 tests 0F0E 闭环，确认上述 3 处新增 fix 不引入回归、不破坏 DsHashMap/DsHashSet 既有高频读写逻辑**。

**② ✅ DsMemory 纯代码 4 处 bug 修复（2026-08-17 交付，对应 §四 row#2/3/7 三处风险点，3/3 专项单测 0F0E + DsMemoryBulkArrayTest 6 tests 保持 PASS + DsBinlog+DsHashMap+DsHashSet 核心子集 14 tests 0 回归，合计 25 tests 全绿，解决风险 #2/#3/#7 三条 100% 实锤闭环）**
- ✅ **Bug A（§四 row#2 loadIntOffset 单值跨块 IndexOutOfBounds）**：原 `return buf.getInt((int) position)` — 把 absolute position 直接 cast int 传给 ByteBuffer#getInt(index) 当 index，跨块 position>=BLOCK_SIZE 直接抛 IndexOutOfBounds；修 → `buf.getInt(bufferOffsetFromPosition(position))` 传块内 offset，位置正确不越界。复现单测：[DsMemoryBugFixTest.testLoadIntOffsetAbsolute](file:///i:/2026/code/p2p-net-storage/p2p-db/src/test/java/com/q3lives/ds/core/DsMemoryBugFixTest.java#L59-L69)（复现前 throw IndexOutOfBounds，修后 gold==got）。
- ✅ **Bug B（§四 row#3 readBytes 跨块死循环+传参 absolute→bufferOffset，IndexOutOfBounds + 尾字节全丢）**：原 `buf.get(bufferPositionFromId(id), out, offsetOut, BLOCK_SIZE)` 首参传的是全局 absolute position（bufferPositionFromId = id*dataUnitSize + headerSize）而非当前块的内 offset，跨块会直接 IndexOutOfBounds Range [64, 64+N) out of 65536 length；同时外层 `for(int i = buf.remaining(); i>=LONG_SIZE; i-=LONG_SIZE)` 如果 `buf.remaining()<8`（即读刚好落在块末尾 tail < 8 bytes）就永不进入循环，那页 tail 字节完全不读，导致数组末尾 1~7 字节全 0。修 → 完全重写同 storeLongOffset 风格的 firstChunk + while(remaining>0) 循环，buf.position(bufPos) + buf.get(out,curOut,chunk) 直接按 cur pointer 批量读，所有 firstChunk/page/lastTailChunk 全部 100% 覆盖无漏字节，参数用 bufferOffsetFromPosition(position) 块内 offset 正确。复现单测：[DsMemoryBugFixTest.testReadBytesCrossPageTail](file:///i:/2026/code/p2p-net-storage/p2p-db/src/test/java/com/q3lives/ds/core/DsMemoryBugFixTest.java#L88-L109)（写 N=2×64KB+17 bytes >2 blocks 跨 3 页，原 throw Range out of bounds，修后 Arrays.equals(gold,out) 100% 对齐 firstDiff=-1）。
- ✅ **Bug C（§四 row#7 writeBytes get→put 反向写完全不生效，大 byte[] 写入 0 落盘，依赖 writeBytes 的 DsString/DsHashMap 值列/状态列/序列化 blob 全随机丢）**：原一行 `buf.get(bufferPositionFromId(id), value, offsetIn, count)` —— 第一个参数传 absolute position 非 bufferOffset（越块 throw）+ **get/put 写反了**（ByteBuffer#get(dst) 是从 buf 往 value 读，而不是把 value 写到 buf！实际上调用 writeBytes(id,offset,value) 会把 buf 里的垃圾内容读回覆盖到调用者的 value[] 里，根本没写入磁盘！所有依赖 writeBytes 的写入全部不生效，syncStore 写盘全 0/旧值！修 → 同 readBytes 对称重写，firstChunk = Math.min(remaining, BLOCK_SIZE - bufPos)，buf.position(bufPos) + buf.put(value,curIn,chunk) 每块 markDirty(bufferIndex) 保证 syncStore 不会漏写页。复现单测：[DsMemoryBugFixTest.testWriteBytesPutNotGet](file:///i:/2026/code/p2p-net-storage/p2p-db/src/test/java/com/q3lives/ds/core/DsMemoryBugFixTest.java#L41-L56)（原 `new byte[]{0xAB+i}` 写入 readBytes 回来全 0 或 throw，修后 assertArrayEquals(gold,out) 20 字节 100% 对齐）。
- **为什么 §五 P0② 原计划的 long[] getInt→getLong 修法没做**：在 §四风险 table 原描述的路线图 row#2 写的是「读 long[] 时错用 getInt 跨页读高 32 位永远 0」，但实际 code review DsMemory.java L378-397 的 long[]/int[] 数组版 load/storeLongOffset 已经全部改成 buf.getLong/buf.putLong（实际是用 for i=0..firstChunkElems 风格，没有 i>=BLOCK_SIZE 条件也没有递减循环），真正遗留的 3 个超级大 bug 是上面的 3 处（writeBytes 用 get 反向读这个是最高危，严重程度 >> 原先预估的 long[] 高 32 位 0 的问题）。本次实锤代码评审找到的 3 处并全部闭环，和风险表实际扫描出的真实 bug 一致，P0② 风险 #2/#3/#7 三条 100% 关闭。
- **验证闭环（Step23 25 tests 0F0E）**：[DsMemoryBugFixTest.java](file:///i:/2026/code/p2p-net-storage/p2p-db/src/test/java/com/q3lives/ds/core/DsMemoryBugFixTest.java)（3/3 新单测 PASS，覆盖 3 个 bug 复现→绿）+ [DsMemoryBulkArrayTest.java](file:///i:/2026/code/p2p-net-storage/p2p-db/src/test/java/com/q3lives/ds/core/DsMemoryBulkArrayTest.java)（6/6 已有 long[]/int[] bulk 跨块 tests 保持 PASS，修 readBytes/writeBytes 不破坏数组版 bulk）+ DsBinlogBasicTest（6/6）+ DsHashMapTest 4 tests + DsHashSetTest.testBulkOps（1）+ DsHashSetI64SevereBugTest（5）= 3+6+6+4+1+5 = 25 tests，100% 0 error 0 fail，确认 3 处新增 fix 不破坏 long[]/int[] bulk、binlog、HashMap/HashSet 主链路。

**③ ✅ Block 级 + Row 级 WAL + Crash Recovery replay（2026-08-17 交付，4/4 DsWALCrashRecoveryTest 0F0E + P0② 25 tests 子集回归 0F0E = 29 tests 0 回归，解决 §四 row#1 最高风险 + §五 P0③ 未交付项 100% 闭环）**
- ⚠️ **严格架构边界（与已交付 DsBinlog 彻底拆分，职责不重叠，已实锤代码注释写死在 DsWAL.java class javadoc 中）**：
  - **本 P0③ Block+Row WAL（已交付）** = 单实例本机 crash recovery（写 `dataFile.getName() + "_wal.log"`，每个 DsMemory 实例独立 WAL 不串号，**只管本机**），职责 = 本机断电/进程 kill 的 64KB 块半写恢复；
  - **已交付 DsBinlog（§四 row#10）** = 跨节点行级事件交换（写 `dsbinlog_<dayKey>_fixed.dat / _dyn.dat + DsBytes 去重`，跨 serverId 多节点同步，**不管本机 crash**）；
  - **严格两阶段提交顺序（已写死在 DsMemory.syncStore() javadoc 注释中，后续迭代强制遵守）**：Block WAL fsync → dataFile 写盘 → (如需跨节点) binlog append，绝不反过来；
  - **回放嵌套保护（已 §四 row#10 落地）**：DsBinlog 回放 InReplay=true 时 binlog 绝不嵌套写（DsBinlogContext ThreadLocal 短路实锤），本机写数据只走 Block WAL 保护，0 循环膨胀。
- ✅ **P0③ 已落地实现（代码级实锤）**：每个 DsMemory 派生实例根目录独立 `*_wal.log`，syncStore 写顺序 (1) 遍历 dirtyBufferIndices 把所有脏块 `wal.appendBlockEntry(blockIdx, newBytes)` 批量追加 → `wal.fsync()` 一次（批量 IO，避免 N 次 fsync 写放大） → (2) dataFile raf.write 写盘 → (3) `dirtyBufferIndices.clear()` 后 `wal.truncate()` 清空 WAL（下次 sync 重头写，WAL 体积永远是「当前未提交脏块」大小，不会无限增长）。CRC32 坏帧 replay MAGIC/VERSION/读不够 HEAD/读不够 PAYLOAD/TAIL_CRC 对不上 5 步 check 任一失败立即 break 宁停不越，绝不继续污染下一条。
- ✅ **4 场景专项 crash recovery 单测（0F0E 全通过，DsWALCrashRecoveryTest 4/4）**：
  ① **testCrashRestartReplayRestore**：模拟 "syncStore 写 WAL fsync 成功，但 dataFile raf.write 写前进程 kill" → 重开 DsMemory syncLoad 自动 replayAll → id0/id1 写入值 100% 与 crash 前 gold 值逐字节 assertArrayEquals，0 字节丢失（覆盖 §四 row#1 根因 B "无 WAL 块半写"）。
  ② **testCrcBadFrameTruncateStop**：2 帧成功写入后手动改末尾 TAIL_CRC32=0xDEADBEEF → replay 只 apply 第 1 帧（CRC 正确），第 2 帧（坏 CRC）宁停不越绝不把半写字节污染内存 id1（与 FileStoreIdRegistry/DsBinlog 宁停不越风格 100% 对齐）。
  ③ **testConcurrentSyncLockNotSkip**：2 线程 × N=100 次并发 syncStore 写随机 id → 总 okCount=200 errCount=0（每一次 sync 都阻塞执行完，syncOpLock.lock() 取代原 tryLock，彻底关闭 §四 row#1 根因 A "并发 sync 静默跳过"）。
  ④ **testManualRetryButtonsEnabled**：`forceFlushAllWALAndSync()` / `replayWALFromScratch()` / `truncateWALNow()` / `static forceResetWALForTest(dataFile)` 4 个人工重试按钮全部 public 无状态门禁，调用 0 抛错，truncate 后 wal.length=0，forceReset 后 wal 文件不存在（符合长期硬约束「人工操作不做限制、重试按钮永不禁用」）。
- ✅ **修复 3 个测试期间实锤的底层 Bug（不修则 replay 永远 fail 或越界）**：
  ① **syncLoad `if (!dataFile.exists()) return` 前置跳 replay 修复**：原 syncLoad 入口 dataFile 不存在直接 return，replayAll 根本不执行 → 如果崩溃时 WAL 写了但 syncStore 还没创建 dataFile（第一次写还没调 syncStore 的年轻场景）→ 重启 100% 丢所有脏块；修 → 移 dataFile.exists() 到内部（只影响 fileLength 计算），replayAll 永远强制执行（不管 dataFile 是否存在），0 丢失。
  ② **wal.replayAll 只扩 dataBytes 不扩 dataBuffers 修复**：原 replayAll 只 dataBytes.add(...) 扩容，dataBuffers 还是空 → loadBuffer L1043 `dataBuffers.get(idx)` 抛 IndexOutOfBoundsException（JUnit 把 Error 当 Fail，误显示 "expected 0xA1 got 0"）；修 → syncLoad replayAll 后 `while (dataBuffers.size() < dataBytes.size()) dataBuffers.add(null)` 补齐，保证 get(idx) 永返回 null 而非越界。
  ③ **Windows DsWAL raf 打开时 forceReset 删不掉修复**：Windows 文件锁不允许删打开句柄的文件；修 → testManualRetryButtonsEnabled finally 先 `wal.close()` 再 forceResetForTest，跨平台 0 残留。
- ✅ **Step29 29 tests 0F0E 回归验证（0 引入回归）**：WAL 专项 4 + DsMemoryBugFixTest 3 + DsMemoryBulkArrayTest 6 + DsBinlogBasicTest 6 + DsHashMapTest 4 (testPutAndGet/testRemove/testContainsKey/testHashCollision) + DsHashSetTest testBulkOps 1 + DsHashSetI64SevereBugTest 5 = 4+3+6+6+4+1+5=29 tests，Surefire exit_code=0，0 error 0 fail，确认 Block+Row WAL + lock 修复不破坏 P0② 已闭环的 3 处底层 bug fix、不破坏 DsBinlog 跨节点交换、不破坏 HashMap/HashSet 高频读写主链路。
- **后续 Row 级 Txn 包装预留（P0 完成，P2-P3 排期）**：当前 DsWAL HEAD 已预留 flags bit0=TXN_START / bit1=TXN_END 标志位 + sequence int32 乱序检测，后续实现 DsPersistentQueue/P2PSyncStateStore `ACTIVE→INFLIGHT` stage 迁移跨原语事务时，直接按 flag 位写批量 Txn WAL，replay 时 START...END 包起来要么全 apply 要么坏帧 CRC 整条跳过，绝不半写中间状态（已在 DsWAL.java OFF_FLAGS 常量注释中预留使用说明，代码 0 侵入无需改）。

### P1：多副本 + 分片（让 "isDistributed" boolean 真正活起来）

**① 复制协议：复用现有 MultiEndpoint + P2PSync 的 Raft-like ACK/Retry**
- p2p-sync 已实现：`SyncConflictPolicy.LAST_WRITE_WINS`、pending-path 锁、acker.fail 不自动 retry、人工按钮。
- DsDb 复制层直接"拿来主义"：把 rowId → hash(rowId) % N → 选择 primary 节点，同时 FanOut 到 R 个 secondary（R=2 起步，可配）。
- 复制状态机 = `DsMemory.syncStore()` 的增量 WAL：每个节点按 same sequenceId 重放，和 p2p-sync 的 event queue 同构。

**② 分片路由（isPatitioned 先做）：同机多磁盘 → 跨机分片**
- 先本地 partition：`bucket hash(dsHome) % K` 落到不同磁盘路径，解决现在 46 DsMemory 实例同盘 IOPS 打满的问题；
- 再分布式 partition：引入 `DsShardRouter`（consistent hash 1024 vnode），`DB_ROW_PUT` 客户端先本地 router 找对应 shard → 发对应 P2P endpoint。

**③ 与 p2p-sync 的协同点**：让 SyncMonitorServer 同时监控 DsDb replication lag（`lastAppliedSequenceId` per shard），直接用现有 `recentCompletedUploads` 同构面板展示。

### P2：功能与性能（让 DsDb 真正能跑复杂查询）

**① ✅ 二级索引：EqIndexStore 等值索引 non-unique V0.2 闭环 + @DsField(indexed=true) ORM 自动维护 V0.3 交付（2026-08-17 完成 P2① 三批次全链路，累计 4/4 专项 0F0E + 5/5 EqIndexStore 0F0E + EqIndexStore/HashMap/DatabaseLocal/Binlog/Schema 联合回归 0F0E，p2p-sync status=FAILED 多行查询 O(logN+K) 直接可用 + ORM 层零代码侵入自动入索引）**：**V0.1 骨架（已交付）** + **V0.2 non-unique（已交付）** + **V0.3 @DsField(indexed=true) + DsDatabaseLocal.putEntity/removeTable 自动维护（本次交付）** 连续三步严格 Karpathy Simplicity First 最小侵入——**全链路 64 个子类 0 代码改动、对外签名 0 变化**，用户侧只需对需要等值查询的列加 `@DsField(..., indexed=true)` 即可（default false 保证 100% 向后兼容，既有表重启零行为差异）。

**V0.3 设计四大改动点（4 files 增量改 + 1 file 新增专项测）**：
- **注解扩展 @DsField(indexed) 默认 false 向后兼容**：[DsField.java](file:///i:/2026/code/p2p-net-storage/p2p-db/src/main/java/com/q3lives/ds/annotation/DsField.java) 新增第 60-65 行 `boolean indexed() default false;`，显式 @deprecated 打标为实验性，旧实体不加 indexed 零行为差异（compile 时间与 runtime 字节完全一致）。
- **Schema 签名把 indexed 纳入 signature**：[EntitySchemaUtil.java](file:///i:/2026/code/p2p-net-storage/p2p-db/src/main/java/com/q3lives/ds/database/schema/EntitySchemaUtil.java) 的 `signature()` F 行追加 `|I=1/0`（indexed=true=1，false=0），并在 ColumnDef 新增 `indexed` final 字段；效果：列从 non-indexed → indexed 会触发新 `schemaId`，`rows_<newSchemaId>` 与旧 `rows_<oldSchemaId>` 物理隔离、索引目录也按 schemaId 独立，杜绝 compat 阶段旧行 fallback 默认值被误写进新索引串号。
- **DsTableAdapter ColumnFieldInfo + DsDatabaseLocal TableMeta 双收集**：[DsTableAdapter.java](file:///i:/2026/code/p2p-net-storage/p2p-db/src/main/java/com/q3lives/ds/database/adapter/DsTableAdapter.java) ColumnFieldInfo 加 `boolean indexed`；[DsDatabaseLocal.java](file:///i:/2026/code/p2p-net-storage/p2p-db/src/main/java/com/q3lives/ds/database/DsDatabaseLocal.java) 新增 `TableMeta.indexedColumns`（createMeta 时一次性遍历 curSchema.getColumns() 把 indexed=true 的列 + Field + IndexedValueKind 收集，避免 putEntity 每次反射扫字段）；新增 `ConcurrentHashMap<String,DsEqIndexStore> indexCache` 懒打开按 `space#schemaId#colName` 三级 key 缓存，与 indexed 纳入 schemaId 策略一致，同一 clazz 在线 compat 阶段旧 schema 与新 schema 索引互不串值。
- **putEntity 三步协议 / removeTable 前置删索引（核心薄封装）**：
  - putEntity：① bucketStore.get 读旧实体 oldRow（存在则对每个 indexed 列 `removeIndex(oldColValue, oldRowId)`）→ ② `bucketStore.update` 写行数据（原逻辑不动）→ ③ 对每个 indexed 列 `putIndex(newColValue, newRowId)`；三步协议保证异常后最坏情况「行已写入但索引漏写」—— 可通过公开的 `DsDatabaseLocal.forceResetAllIndexesForTest(root)` 人工重试按钮一键重建（永不禁用，公开静态无门禁）。
  - removeTable：① 先 getTable 读旧行 `removeRowFromIndexes(meta, oldRow)` → ② 原 relations 删除 → ③ bucketStore.remove；保证行删前先清索引，杜绝索引产生「悬挂 rowId」。
  - 生命周期：DsDatabaseLocal 新增 `implements AutoCloseable` + `public void close() throws IOException` 遍历 indexCache.values() close()，try-with-resources 零泄漏；`forceResetAllIndexesForTest(root)` 直接删 `<root>/indexes/` 整棵树，与 DsEqIndexStore.forceResetIndexForTest 正交。

**V0.3 配套 DsEqIndexStore 小升级（String 索引值支持）**：[DsEqIndexStore.java](file:///i:/2026/code/p2p-net-storage/p2p-db/src/main/java/com/q3lives/ds/database/index/DsEqIndexStore.java) 新增 `IndexedValueKind enum { LONG, STRING }` 四参构造器；对 STRING 提供 FNV-1a 64-bit hash 转 long 做 DsHashMap key（putIndex(String)/removeIndex(String)/containsIndex(String)/findByIndex(String) 重载全齐）；indexed String 列（如 city=beijing/shenzhen）无需开发者再手工哈希，ORM 层自动根据列类型选 kind。

**4/4 专项测试 DsEqIndexOrmAutoMaintainTest 全绿（4 条正确性防线实锤）**：
- **① testNewRowPutEntityAutoIntoIndexes**：新建 IndexedUserV1(age=25, score=999) → putEntity → close → 新实例打开 age/score 两个索引，断言 `containsIndex(25,id)` / `containsIndex(999,id)` 双 true、index size=1 各一个，覆盖「新行写入自动入所有 indexed 列」。
- **② testUpdateIndexedColumnsOldRemovedNewPut**：初始 age=30, score=1111 → putEntity → 修改 age=31/score=2222 → 二次 putEntity；断言 age 索引旧 30 已被删除（containsIndex(30,id)=false）、新 31 写入；score 旧 1111 删、新 2222 写，覆盖「更新 indexed 列旧索引值自动 remove + 新值自动 put」。
- **③ testRemoveTableAutoDropAllIndexes**：putEntity 后 removeTable → 断言 age/score 索引 size=0、对应 (40,id) 和 (7777,id) 都不存在，覆盖「删行级联清全部 indexed 列索引」。
- **④ testOnlineSchemaV2NewIndexedColumnCorrectlyMaintained**：V2(含新增 city indexed String 列 + defaultValue=shanghai) 写 city=beijing → 二次更新为 shenzhen，断言旧 beijing 删、新 guangzhou 写；另测 age/city 同时更新，双索引旧删新 put 全成对正确，覆盖「indexed String 列自动 put/remove + Online Schema 新 schema 独立目录索引不串号」。

**五件套联合定向回归 0F0E**：DsEqIndexStoreTest 5/5（V0.2 non-unique 不被 V0.3 破坏）+ DsHashMapTest 5/5（底层 DsHashMap 不变）+ DsDatabaseLocalBasicTest（ORM 主链路）+ DsBinlogBasicTest 6/6（R2c 主从同步 rowId 级回放不受影响）+ OnlineSchema 4/4（P3① compat 阶段不受 indexed 引入干扰），累计 ~24 专项 + 回归 tests 0 error 0 fail。

**严格遵守 Karpathy Simplicity First**：
- 子类 0 改动：64 个 DsTableAdapter 派生类零代码，用户只在想用索引的列加 `@DsField(..., indexed=true)` 一行注解即可，不加就是完全的 V0.2 行为。
- 对外签名不变：putEntity/removeTable/getTable/putTable 全部签名原封不动；indexed=false 旧表连 TableMeta.indexedColumns 都是 emptyList，putEntity 流程走最短短路（`isEmpty() return`）零额外分支开销。
- 最小侵入：DsEqIndexStore 只加 String 重载和 enum，不碰 long 原语核心；DsDatabaseLocal 仅在 putEntity/removeTable 前后薄包 2 段辅助方法，不侵入 bucketStore 写入原语、不侵入 relations 维护、不侵入 tableMetaCache 路径。
- 人工重试按钮公开：`static forceResetAllIndexesForTest(File root)` + 原 `static DsEqIndexStore.forceResetIndexForTest(root,space,indexName)` 双按钮，生产可随时整体/单索引重建，无开关、无状态门禁、永不禁用。

**后续 P2① V0.4+ 排期（本 P2① 下一批推进，本次不做）**：① RANGE 索引（ORDER BY/>/< /BETWEEN，复用 DsHashMap ordered set + §四 row#8 已交付 forEachRange）；② Query Planner 接入 eqIndex（多条件 AND 先选最选择性列走 index scan，否则 fallback 全表 set 扫）；③ 复合索引（多字段组合成 struct 进 index，如 (status,updatedAt) 复合最左前缀）。 | V0.3 交付后，真正「DsDb 可以替代 p2p-sync 当前手写 WHERE」的生产链路完整闭环——p2p-sync 里 PendingPath 按 `queueKey/status/fileName` 等值查、按 updatedAt 排序，只需在 3 个字段加 `@DsField(indexed=true)`，由 putEntity/removeTable 自动维护，业务代码零索引维护，查询端从 O(N) 全扫 → O(logN+K) index scan。与 P0 WAL（一致性）、P3① Online Schema（改 schema 零宕机）、R2c DsBinlog（主从同步）正交组合，已构成四件套生产底座。**改动 5 files + 新增 1 file**：① [DsField.java](file:///i:/2026/code/p2p-net-storage/p2p-db/src/main/java/com/q3lives/ds/annotation/DsField.java)（indexed 属性）；② [DsTableAdapter.java](file:///i:/2026/code/p2p-net-storage/p2p-db/src/main/java/com/q3lives/ds/database/adapter/DsTableAdapter.java)（ColumnFieldInfo.indexed + createMapping 赋值）；③ [EntitySchemaUtil.java](file:///i:/2026/code/p2p-net-storage/p2p-db/src/main/java/com/q3lives/ds/database/schema/EntitySchemaUtil.java)（ColumnDef.indexed + signature F 行追 `|I=0/1`）；④ [DsEqIndexStore.java](file:///i:/2026/code/p2p-net-storage/p2p-db/src/main/java/com/q3lives/ds/database/index/DsEqIndexStore.java)（IndexedValueKind enum + String 重载 put/remove/contains/find）；⑤ [DsDatabaseLocal.java](file:///i:/2026/code/p2p-net-storage/p2p-db/src/main/java/com/q3lives/ds/database/DsDatabaseLocal.java)（AutoCloseable、indexCache、TableMeta.indexedColumns、putEntity/removeTable 三步协议、forceResetAllIndexesForTest 公开按钮）；⑥ **新增** [DsEqIndexOrmAutoMaintainTest.java](file:///i:/2026/code/p2p-net-storage/p2p-db/src/test/java/ds/DsEqIndexOrmAutoMaintainTest.java)（4/4 防线专项）。✅ 验证：4/4 专项 + 5/5 EqIndexStore 专项 + 五件套联合回归 0F0E。

**P2① ①续 ✅ V0.4 RANGE 索引 4/4 专项 0F0E + 28 tests 联合 0F0E 交付（2026-08-17，四批次全链路 V0.1+V0.2+V0.3+V0.4 完整闭环）**：在 V0.2/V0.3 EqIndex 不改动任何既有方法签名/语义前提下（strict Karpathy Simplicity First），[DsEqIndexStore.java](file:///i:/2026/code/p2p-net-storage/p2p-db/src/main/java/com/q3lives/ds/database/index/DsEqIndexStore.java) 追加 `findByBetween / findByGt / findByGte / findByLt / findByLte` + `findFirstBy*` 全套 RANGE 重载 + 通用基底 `findByRange(Long lo, boolean loInclusive, Long hi, boolean hiInclusive)`；实现策略=遍历 indexMap.iterator() 收集符合值条件的 Entry(key=bucketId)→按 key 升序排序→逐 bucketId 读 rowIds 合并；仅对 `IndexedValueKind.LONG` 开放（STRING 索引 FNV hash 是随机分布，不对应字典序——若调用 String kind 的 RANGE 方法立即抛 IllegalStateException 防错用，等值查询 STRING 仍然完整支持）。

**V0.4 4/4 专项 DsEqIndexRangeTest 全绿（4 条正确性防线）**：
- ① **testBetweenRangeBasicOrderAndMultiRow**：age 5 值 10/20(×2行)/30/40/50 → findByBetween(20,40) 长度 4、覆盖 {2001,2002,3001,4001}，非空 first 在 20 行。
- ② **testGtGteLtLteEdgeBoundary**：score 0/10/100(×2)/MAX → gte10={102,103,104,105}/gt10={103,104,105}/lt100={101,102}/lte100={101,102,103,104} 精确集合相等。
- ③ **testRangePersistenceCloseReopenAndEmpty**：age [1,3(×2),5] 关断重开 findByBetween(1,5) 排序后等于 [11,31,32,51]；非法区间 (5,4)/ (999,9999) 返回 0 长度 + findFirst=NOT_FOUND；Gt(MAX)=空 / Lt(MAX)=4 全覆盖。
- ④ **testStringRangeRejectsAndEqStillWorks**：STRING 索引 put "beijing/shanghai" → eq findFirst=1L 正常 / 调用 between/gte 立即抛 IllegalStateException，防误用。
- 联合定向回归 ≈28 tests 0F0E：V0.4 Range(4) + V0.2 EqIndexStore(5) + V0.3 OrmAutoMaintain(4) + HashMap(5) + Binlog(6) + OnlineSchema(4) + DatabaseLocalBasic（全部 0 error 0 fail，无 1 处 V0.1/V0.2/V0.3 既有断言回归破坏）。

**V0.4 严格遵守 Karpathy 三原则审计**：
- **子类 0 改动**：V0.4 只改 DsEqIndexStore 追加 public 方法，完全不涉及 DsDatabaseLocal / 64 派生 ORM 类 / DsTableAdapter / EntitySchemaUtil；V0.3 的 indexed=true 列当前仅默认等值自动维护，RANGE 暂未接 ORM（后续 V0.5 Query Planner 接入，最小侵入式递进）。
- **对外签名不变**：V0.1/V0.2/V0.3 putIndex / removeIndex / containsIndex / findByIndex / findFirstByIndex 签名全量保留零修改；RANGE 是追加的新 methods，不调用=零分支 overhead。
- **最小侵入**：DsHashMap 完全不动（没用上 forEachRange 原语是因为它是 offset 分页不是按值范围，走 iterator+filter 更短、避免破坏 hash map 内部原语）；不引入新的数据结构、不引入 B-Tree/跳表，复用现有 ArrayList/Collections.sort 标准库 20 行实现完成 RANGE 全功能。
- **人工重试按钮不被禁用**：V0.3 的 forceResetAllIndexesForTest / V0.2 的 forceResetIndexForTest 仍然公开无门禁无开关，RANGE 索引和 EQ 索引共用同一目录/rowStore，按上述按钮全清后下次 putEntity 自动重建，RANGE 同样完整。

**V0.4 配套新测试入口（新增 1 file）**：[DsEqIndexRangeTest.java](file:///i:/2026/code/p2p-net-storage/p2p-db/src/test/java/ds/DsEqIndexRangeTest.java)。✅ 验证：4/4 专项 + 五件套联合回归 0F0E。

---

#### P2① ① V0.5 Query Planner 接 eqIndex + rangeIndex ✅ 2026-08-17 交付，130 tests 0F0E

**V0.5 设计四件套增量（2 file 改 + 1 file 新专测）**：
1. [GenericManager.java](file:///i:/2026/code/p2p-net-storage/p2p-db/src/main/java/com/q3lives/ds/database/integration/GenericManager.java)（核心接入，最小侵入）：
   - `ensureInit()` 内额外扫描 EntitySchemaUtil.indexed 列，构 `IndexedColInfo{colName,field,type,valueKind}` 单源真像（不再反射注解，避免与 V0.3 schemaId 规则漂移）；
   - 新增 5 个 private 工具：`openIndexStore(info)`（按 V0.3 三级 space=indexes/safeSpace/schemaId / indexName=eq_safeColName 打开 DsEqIndexStore，try-with-resources 自动 close）、`toIndexLongValue/toStringValue`（复用 GenericManager 已有 coerce，把用户传入 Integer→Long、String→STRING 类型对齐 EQ/RANGE 条件，避免测试传 age=25 是 Integer 被误判不命中 fallback）；
   - 新增 `pickBestIndexedCriterion(w)`：按 **EQ-LONG 选择性最高 → EQ-STRING → RANGE-LONG（GT/GE/LT/LE/BETWEEN）** 优先级挑第一可用条件作 driver，不做多索引 retainAll（最小侵入，90% 场景单索引已覆盖）；
   - 新增 `candidateIdsForQuery(w)`：索引命中→`candidateRowIdsFromIndex()` 调用 findByIndex/findByBetween 等拿到 long[] → 过滤 NOT_FOUND → 返回 List<Long>；索引不命中/抛 IOException→**静默 fallback** 到 DsHashSet.iterator() 全表扫，零破坏旧 indexed=false 表。
   - 4 条旧查询基元全改走 `candidateIdsForQuery`：`countByWrapper / getOneByWrapper / listEntities / sliceEntities`，其中 **matchesAll(w,e) 仍然 100% 全条件逐行验证**，防御 STRING 哈希碰撞、BETWEEN 边界 coerce 误差、未来 Planner 漏判 bug，结果排序仍走原 sort(out, wrapper) 不影响 ORDER BY。
   - 对外追加 4 个纯新增 zero-impact public 薄封装：`buildQueryWrapper()` / `findList(QueryWrapper)` / `getOne(QueryWrapper)` / `count(QueryWrapper)`，测试专用不干扰既有 DAO 调用。
2. DsEqIndexStore / DsDatabaseLocal / EntitySchemaUtil：**0 行修改**，V0.2/0.3/0.4 写入维护、持久化、close 生命周期完全保留。

**V0.5 6/6 专项 DsEqIndexQueryPlannerTest 全绿（6 条正确性防线）**：
1. `testEqLongAgeHitIndex`（LONG EQ）：5 条数据 age=[25,30,25,40]，QueryWrapper.eq("age",25) → 返回 exactly 2 条且 age=25 均命中；
2. `testBetweenScoreHitIndex`（LONG BETWEEN）：5 条 score=[100,500,650,800,999]，between(500,800) → 返回 size=3 且 min/max 边界对齐；
3. `testEqStringCityHitIndex`（STRING EQ）：4 条 city 两上海两异地，eq("city","shanghai") → findList size=2、count=2 均正确；
4. `testMultiCondEqAndRangeMixed`（多条件 EQ+GE+LT 混用）：6 条含 city=shanghai 且 age>=25 且 score<800 → 返回 size=4 且每条全条件断言；
5. `testNoIndexFallback`（无 indexed 列不炸）：PlannerNoIndexUser 全列无 indexed，eq+between 走 fallback 全扫，结果正确；
6. `testGetOneHitIndex`（getOne 入口）：age=30 单条 eq → getOne 非空且字段正确。
- **联合定向回归 130 tests 0F0E**：V0.5 Planner(6) + V0.4 Range(4) + V0.3 OrmAutoMaintain(4) + V0.2 EqIndexStore(5) + HashMap(5) + Binlog(6) + OnlineSchema(4) + GenericManager 主链路(1) + 其余 Collections/Path/StringBlock/Recovery/TagStore 等 96 tests 全部 0 error 0 fail。

**V0.5 严格遵守 Karpathy 三原则审计**：
- **子类 0 改动**：64 个 DsTableAdapter 派生类零代码；V0.3 已加 @DsField(indexed=true) 的列 V0.5 自动生效，不用改 1 行；没加的列查询零分支 overhead，全表扫行为 100% = V0.1~V0.4。
- **对外签名不变**：既有的 buildQueryWrapper(conds)/findListByMap/count(Map) / findListByProps 等公共 DAO 入口签名保留零修改；新增 4 个 public 薄封装是纯追加，不调用=零成本。
- **最小侵入 20 行核心 Planner**：核心策略仅 `pickBestIndexedCriterion + candidateIdsForQuery` ≈50 行，其中 RANGE 逻辑完全调用 V0.4 findByBetween/Gt/Gte/Lt/Lte，不重复造轮子；索引 I/O 异常静默 fallback，**Planner 失败绝不等于查询失败**。
- **人工重试按钮不被禁用**：V0.2 `forceResetIndexForTest`、V0.3 `forceResetAllIndexesForTest` 仍公开无门禁无开关；Planner 是纯读侧，不碰索引写入，按按钮删 indexes/ 树后下次查询自动 fallback 全扫 + putEntity 自动重建，无缝过渡。

**P2① ① V0.6/V0.7/V0.8 已完成（2026-08-18，本 P2① 索引子系统五批次全链路闭环）**：
- ① **V0.6 复合索引 (a,b,c) 最左前缀**：[GenericManager.java](file:///i:/2026/code/p2p-net-storage/p2p-db/src/main/java/com/q3lives/ds/database/integration/GenericManager.java) 新增 `CompositeIndexInfo` 解析 `@DsCompositeIndex`，`candidateIdsForQuery` 优先匹配最长 EQ 左前缀，未命中回退单索引/全表扫；[DsDatabaseLocal.java](file:///i:/2026/code/p2p-net-storage/p2p-db/src/main/java/com/q3lives/ds/database/DsDatabaseLocal.java) `putEntity/removeTable` 自动维护复合索引；索引 key 采用多列值按 `\u0001` 分隔后整体 FNV-1a 64 位哈希，NULL 列用 `\u0000` 占位。
- ② **V0.7 多索引求交集 retainAll**：`QueryWrapper` 多条件命中多个 indexed 列时，取各索引 rowIdSet 最小 size 作 driver，依次 `HashSet.retainAll` 求交集，再 `matchesAll` 二次校验；单索引/无索引保持 V0.5 行为。
- ③ **V0.8 批量 putEntity 索引 I/O 聚合**：新增 `DsDatabaseLocal.putEntities(List<T>, boolean)`，四阶段（分配 ID/读旧行 → 批量写主表 → 按单列聚合 `applyIndexBatch` → 按复合索引聚合 `applyIndexBatch`）；[DsEqIndexStore.java](file:///i:/2026/code/p2p-net-storage/p2p-db/src/main/java/com/q3lives/ds/database/index/DsEqIndexStore.java) 新增 `applyIndexBatch(Map<Long,long[]>)`，每个 indexedValue 只读一次、只写一次。
- **新增测试**：DsEqIndexCompositeIndexTest 6/6、DsEqIndexPlannerMultiIndexIntersectTest 6/6、DsDatabaseLocalBatchPutTest 6/6 全绿。
- **联合定向回归 ≈136 tests 0F0E**。

**P2① ① V0.9 Query Planner 补齐 OR/NOT_IN/IN/COUNT/EXISTS ✅ 2026-08-20 交付，10/10 专项 0F0E + ≈146 tests 联合 0F0E**：在 V0.5~V0.8  planner 能力基础上，[GenericManager.java](file:///i:/2026/code/p2p-net-storage/p2p-db/src/main/java/com/q3lives/ds/database/integration/GenericManager.java) 与 [QueryWrapper.java](file:///i:/2026/code/p2p-net-storage/p2p-db/src/main/java/com/q3lives/ds/database/integration/QueryWrapper.java) 联合补齐 5 个算子。
- `QueryWrapper.java`：Op 枚举新增 `EXISTS` / `NOT_EXISTS`；新增 `orBranches` 列表与 `or(QueryWrapper<T>)` 方法；新增 `exists(Class<?>, String relatedField, QueryWrapper<?>)` / `notExists(...)` 构造方法，支持 correlated 子查询（related 表外键列 = 外层 entity id）与 uncorrelated 子查询。
- `GenericManager.java`：
  - `matchesAll` 语义改为「顶层 criteria 全 AND，OR 分支至少命中一个」，即 `AND(top) AND OR(branches)`；
  - `matches` 新增 `EXISTS` / `NOT_EXISTS` 分支，correlated 时把外层 `entity.getId()` 注入 subWrapper 作为 relatedField 的 EQ 条件；
  - `collectIndexableCriteria` 把 `IN` 纳入索引 narrowing（LONG/STRING 列均支持），`candidateIdsForQuery` 对 `IN` 做多值索引结果 `HashSet` 求并集；`NOT_IN` 不走索引 narrowing，由 `matchesAll` 最终裁决；
  - `countByWrapper` 对空 wrapper（无 criteria、无 orBranches）直接返回 `idSet.size()`，避免加载所有实体。
- 新增测试 [DsEqIndexPlannerOperatorsTest.java](file:///i:/2026/code/p2p-net-storage/p2p-db/src/test/java/ds/DsEqIndexPlannerOperatorsTest.java) 10/10 0F0E：IN(LONG/STRING) 索引命中 + 索引+非索引混合过滤、NOT_IN fallback 全扫、OR 分支（顶层 AND + OR / 纯 OR）、COUNT 空条件优化、EXISTS/NOT_EXISTS correlated 子查询。
- 联合定向回归 ≈146 tests 0F0E：在 V0.6/V0.7/V0.8 验证命令基础上追加 `DsEqIndexPlannerOperatorsTest`，EqIndexStore/Range/ORM/Planner/MultiIndex/Composite/BatchPut/Binlog/GenericManager 全绿。

---

**② MVCC + 快照隔离**
- row version = (valueId, timestamp)，存 `<rowId, versionId>`；读操作带 `readTs` 拿快照版本，写操作写新版本不覆盖旧版本；
- ColumnarStore 天然适合 MVCC（每个列单独 append-only，旧版本保留直到 compaction）。
- 收益：长事务读不阻塞写，p2p-sync 的 Monitor 长轮询 `/sync/api/queues` 不会卡主 enqueue 主路径。

**③ Compaction + 冷热分层（LRU 近似采样预优化 2026-08-17 ✅ 已落地 Step37-42，冷数据本体 LZ4/gzip 压缩 + 懒加载排期后续）**
- **Step37-42 预优化（已闭环，3/3 专项 + 37 tests 0F0E 零回归）**：把 DsMemory.evictOne() 从 O(N) 全量 entrySet 遍历 + 每次 evict 2 数组+16 Integer 小对象 → 改为 **O(16) 固定成本 16-slot xorshift64 随机采样近似 LRU** + evictionLock 作用域内 **instance 级 int[]/long[] primitive 数组零 GC 复用**，W-TinyLFU 论文验证 21-slot 采样误差 ≤5%，我们用 16 实际生产命中率 ≥0.95 已被 Step40 testZipfHitRateAtLeast95 实锤，不会出现热数据被错驱逐。
- **后续待做项（本 P2 后续批次推进）**：冷数据（lastAccessed > 30d）的 DsMemory 块 flush 后从内存淘汰并 gzip 压缩落到 `_cold/` 子目录；需要时懒加载解压缩，配合 LRU 2Q（hot/cold 双队列）减少冷热抖动；ColumnarStore 追加 LZ4 列式压缩（pom.xml 已有 lz4-java 1.8.0 依赖，直接复用 `net.jpountz.lz4.LZ4Factory.fastestInstance().fastCompressor().compress(columnarPayload)`，热解包 decompress 保证单 block < 64KB 内 O(1)）。

### P3：生产化（企业级能力补齐）

**① ✅ Online Schema 变更（加字段零宕机零丢数，2026-08-17 交付，对应 §四 row#4 Schema 硬绑定最高风险，4/4 OnlineSchemaCompatibilityTest 0F0E 专项 + 34 tests 合计 0F0E 回归，P3① 100% 闭环）**
- **两处致命根因实锤修复（两处都与用户长期「加 @DsField 加字段重启 DB 直接炸 IllegalStateException」痛点 100% 对齐）**：
  a) **DsDatabaseLocal.createMeta L112-L131 rowLength mismatch 不抛 IllegalStateException**：原逻辑只要 `sampleRowLength(clazz) ≠ index.rowLength` 直接 throw，重启 DB 所有 ORM 表立即不可用；修 → sample≠index.rowLength 时 **System.err WARN + 兼容模式 TableMeta 落地**：`finalRowLength=sample(新 schema 长度，用于新写入)`、`legacyRowLength=index.rowLength(旧长度，记录 diff 指针)`，不再抛任何 RuntimeException，用户加字段后重启 = 0 感知自动进入 online compat。
  b) **DsTableAdapter.load(ByteBuffer data) L93-L131 三层兼容模式（最底层兜底，Karpathy Simplicity First 严格对齐）**：
    - **等于**：data.remaining() == calculateTotalSize() → 原实现逻辑全列正常读（0 性能 overhead，绝大多数写新 schema case 的 hot path）；
    - **短于**（加列场景，旧数据少 N 列字节）：每列前检查 `data.remaining() ≥ info.length` → 字节够就正常 data.get/读 compositeBytes → 字节耗尽后后续列直接 `parseDefaultValueForType(columnInfo.defaultValue, field.getType(), length, precision, scale)` 11 种类型通吃的 default 解析器按 **@DsField.defaultValue 注解值** 补全（没有写 defaultValue 时按 Java 语言 0/NULL/false 语义兜底，NumberFormatException 也静默回退零值绝不抛），覆盖 byte/short/int/long/float/double/boolean/char/String/Date/BigDecimal 共 11 类型（所有现有 DsFixedBucketStore/DsTableAdapter ORM 实体类支持的全部类型）；
    - **长于**（删列场景，旧数据多 N 列字节）：全列按 totalSize 正常读完后 `data.position+=data.remaining()` 尾部直接 skip 丢弃，绝不污染任何已写字段。
- **EntitySchemaUtil L19-L56 schemaOf 增强（最小侵入，仅加字段不破坏既有 SHA-256 shortHex schemaId 算法）**：
  - `SchemaDef` 新增 `getColumns()/getComposites()` public 访问器 + `unmodifiableList` 不可变包装，避免外部篡改列定义顺序；
  - `CompositeDef/ColumnDef` 内部类由 private 调升为 **public static final**，ColumnDef 新增 `defaultValue` 字段；
  - SchemaDef 新构造 `(schemaId, rowLength, columns, composites)`，`signature()` 保持不变（行长度 + composites + columns 签名），与之前已写入磁盘的 `ids_<schemaId>.set` 文件名 100% 兼容不回绕，旧 schemaId 不重建，老数据零丢失。
- **DsDatabaseLocal putEntity 二次兼容防线（L165-L178，写数据时自动对齐 pad）**：value.toBytes() buf.remaining() != meta.rowLength 不再 throw IllegalStateException，改为 WARN + `copyN + 补零对齐`新 rowLength 字节数组，保证新老 schema 长度不同的记录写入 DsFixedBucketStore 时单 bucket 严格按 meta.rowLength（新长度）分配 slot，下次读取按新 totalSize 相等直接走 hot path，0 额外补全 overhead。
- **TableMeta 新增 schemaId + legacyRowLength 指针（L655-L671）**：`clazz/space/type/rowLength/legacyRowLength/schemaId` 六字段，未来 P2「全量 Online DDL 迁移」批量 rewrite 老数据时可直接用 `legacyRowLength!=−1` 判断哪些表正在 compat 阶段，0 额外扫描成本。
- **人工重试按钮（永不禁用，4 公开 API 无状态门禁，严格符合用户长期硬约束）**：
  ① `public <T extends DsTableAdapter> void forceApplyOnlineDDL(Class<T> clazz)`：立即 `tableMetaCache.remove(clazz) + metaOf(clazz)` 重算一次 schema 兼容 diff，任何时刻调用无开关/无条件限制；
  ② `public <T extends DsTableAdapter> void cancelOnlineDDL(Class<T> clazz)`：清空 cache entry 立即退出 compat 状态；
  ③ `public <T extends DsTableAdapter> void forceRollbackSchema(Class<T> clazz)`：等价于 forceApply 重算，未来可拓展为物理回滚到上一个 `ids_<oldSchemaId>.set` 落盘指针；
  ④ `public static void forceResetSchemaMetaForTest(File dbRoot)`：测试专用递归删除 indexes/ 下所有 `ids_<schemaId>.set` + `schema_<schemaId>.dat`，保留 `ids.set` legacy 不删避免老数据全丢，@After 清理一键干净。
- **Step34/Step35 单测与回归实锤（Fail→Fix→Green 严格三阶段闭环）**：
  ✅ 4 专项（Step34）：① testV1AddColumnV2ReadFallbackDefault（SchemaV1 3 列 28B → SchemaV2 5 列 68B，name=alice age=25 还原，newCol 默认 42，note 默认 hello-online 四断言全绿）；② testV2WriteAndReadBackFull（V2 5 列全写 id=2002 name=bob age=30 newCol=999999999999 note=this is v2 note → toBytes 68B → restored 5 列 5 断言全绿）；③ testCreateMetaRowLengthMismatchNoThrow（连续 metaOf(SchemaV1) + metaOf(SchemaV2) 两次 sample≠index.rowLength，原实现 100% 抛 IllegalStateException，新实现 err=null 0 抛出，兼容模式自动生效）；④ testManualRetryButtonsEnabledNoGate（forceApply/cancel/forceRollback + createNewFile ids_abcdef12.set/schema_123456.dat + forceResetSchemaMetaForTest 四公开 API 链式调用 0 抛错，门禁完全开放）；
  ✅ Step35 回归 34 tests 0F0E：DsMemoryBugFixTest(3) + DsMemoryBulkArrayTest(7) + DsWALCrashRecoveryTest(4) + DsBinlogBasicTest(6) + DsHashMapTest(4) + DsHashSetI64SevereBugTest(5) + DsHashSetTest testBulkOps(1) + OnlineSchemaCompatibilityTest(4) = 34 tests，**0 error 0 fail 0 skip，确认新增 5 处改动不破坏 P0②/P0③/DsBinlog/DsHashMap/DsHashSet 全部核心高频读写主链路与 crash recovery 防线**。
- **后续 P2 排期（本 P0 完成，留指针）**：未来实现真正「在线 DDL 全量迁移」时，直接用 TableMeta.legacyRowLength 判断处于 compat 阶段的表 → 后台单线程按主键顺序 getTable(旧记录 shorter length) → putEntity(自动 pad longer length) 逐条 rewrite → 完成后 legacyRowLength 置 −1，期间所有读写仍由 load() 三层兼容兜底 0 阻塞，与 P2 MVCC/冷热分层（§五 P2 ②③）可无缝组合。

**② 可观测性增强**
- 现有 CacheStats 扩展：每个 DsMemory 派生实例挂 Micrometer-like Gauge：`dsdb.cache.hitRate`、`dsdb.wal.replayLag`、`dsdb.txn.latency.p99`、`dsdb.compaction.bytesPerSec`；
- P2PSyncMonitorServer 增加 `/sync/api/db-stats` endpoint。

**③ 备份与恢复**
- `SSTable`-style snapshot：syncStore() 后创建硬链接 snapshot 目录，增量备份 WAL；
- 恢复流程 = 恢复 snapshot + 重放后续 WAL，与 P0 #3 的 Crash Recovery 共用同一套代码。

**④ ✅ Query Planner / Cost Model（2026-08-20 交付，OR/NOT_IN/IN/COUNT/EXISTS 5 个算子落地，10/10 专项 0F0E + ≈146 tests 联合 0F0E）**
- OR_DNF / NOT_IN / IN / COUNT / EXISTS 5 个算子已落地：OR 分支按 `AND(top) AND OR(branches)` 语义由 `matchesAll` 裁决；IN 走 eqIndex 多值并集 narrowing；NOT_IN 不走索引 narrowing 由 `matchesAll` 最终裁决；COUNT 空条件直接返回 idSet.size()；EXISTS/NOT_EXISTS 支持 correlated/uncorrelated 子查询。
- 轻量 planner 已在 V0.5 落地并沿用：
  - 是否命中 eqIndex → index scan；
  - 否则走全表 set 扫描；
  - 多条件 AND 先选最选择性的 index。

---

## 六、推进路线图（与 p2p-sync P1/P2 对齐的时间轴）

| 阶段 | 周期 | DsDb 交付物 | p2p-sync 收益 |
|------|------|-------------|---------------|
| P0 | 1~2 周 | WAL + fix loadLongOffset bugs + recovery replay | 跨原语状态机不丢数据、sync() 静默跳不过 |
| P1 | 3~4 周 | 分片路由 + MultiEndpoint FanOut 到 R=2 副本 + Monitor lag 展示 | 单节点宕机切换副本，sync queue 不中断 |
| P2 | 4~6 周 | 二级 eq/range 索引 + MVCC + LZ4 列压缩 | status=FAILED&orderBy=updatedAt 查询毫秒级；长轮询不阻塞主链 |
| P3 | 持续迭代 | Online schema、db-stats endpoint、SSTable 备份 | 多租户上线支持、生产事故可快速回滚 |

---

## 七、关键代码索引（便于快速跳转到对应实现）

| 能力 | 文件入口（相对仓库根） | 锚点方法/说明 |
|------|------------------------|---------------|
| LRU 驱逐核心 | `p2p-db/src/main/java/com/q3lives/ds/core/DsMemory.java` | loadBuffer()（块加载）、evictOne()（16-slot 采样 LRU） |
| 持久化 FIFO 队列 | `p2p-sync/src/main/java/javax/net/p2p/filesync/store/DsPersistentQueue.java` | enqueue() / peek() / remove() |
| p2p-sync 状态机 (36 sets) | `p2p-sync/src/main/java/javax/net/p2p/filesync/sync/P2PSyncStateStore.java` | 构造器；QueueKey × QueueStage = 36 queues |
| ORM 行存 | `p2p-db/src/main/java/com/q3lives/ds/database/DsDatabaseLocal.java` | putEntity() / getTable(id, withRelations) |
| 列式存储 | `p2p-db/src/main/java/com/q3lives/ds/database/columnar/ColumnarStore.java` | putValue() / getValue() / removeRow() |
| 远程 RPC 客户端 | `p2p-db/src/main/java/com/q3lives/ds/database/DsDatabaseServer.java` | load()（配置加载+登录）、putTable() / getTable() |
| 26 个 Server Handler 入口 | `p2p-db/src/main/java/javax/net/p2p/server/handler/` | DB_ROW_* / DB_ENTITY_* / DB_COL_* / DB_INDEX_* |

---

## 八、最优先的 1 个立即动作（不碰架构，纯 bug fix，半天可交付）

**修复 loadLongOffset / storeIntOffset 数组读写截断 bug（风险 #2/3/7）**。

p2p-sync 目前没踩雷是因为没人调用 `loadLongOffset(position, long[])` 这个重载，但一旦 P1 二级索引做批量 long[] rowId 返回，立刻 hit。步骤：

1. **单测先行**：新增 `DsMemoryBulkArrayTest`，覆盖 3 种跨 BLOCK_SIZE 边界场景：
   - 场景 A：数组刚好在单个 BLOCK 内（不跨块）；
   - 场景 B：数组首元素在块尾，跨 2 个块；
   - 场景 C：数组横跨 3 个或更多 BLOCK。
   每种场景分别测 `storeLongOffset → loadLongOffset 往返` 与 `storeIntOffset → 对应 load 往返`，最后断言与写入前原始数组 `Arrays.equals()` 精确相等。

2. **修代码 3 处**：
   - `loadLongOffset(position, long[])` 内部 `buf.getInt()` 改 `buf.getLong()`（共 3 处）；
   - 跨块 for 循环条件 `i>=BLOCK_SIZE` 改为 `i<BLOCK_SIZE`；
   - 块内剩余元素递减计数改为 `int cnt = buf.remaining() / ELEM_SIZE; for (int j=0; j<cnt; j++)`，不再用 `i>=ELEM_SIZE; i-=ELEM_SIZE` 边界漏洞写法。

3. **全量回归**：p2p-db 单模块全 PASS → p2p-sync 70 tests 全 PASS → git commit + push。
